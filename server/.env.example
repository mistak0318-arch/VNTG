# 키움 REST API 포털(openapi.kiwoom.com)에서 발급받은 값
# (계좌평가/보유종목 조회 API는 앱키에 연결된 계좌를 자동으로 사용하므로 계좌번호는 별도로 필요 없습니다)
KIWOOM_APP_KEY=
KIWOOM_APP_SECRET=

# 실전투자: false, 모의투자: true
KIWOOM_IS_MOCK=false

# 이 서버가 뜨는 포트 (프론트엔드가 이 주소로 API를 호출함)
PORT=4000
