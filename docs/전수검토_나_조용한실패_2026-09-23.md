# 전수검토 2회차 — (나) 조용한 실패 (2026-09-23 08:20~09:00)

> 의뢰서: `docs/전수검토_의뢰_2026-09-23.md`. 이번 회차부터 **찾은 것 중 실측·코드근거가 있는 것은 바로 고쳤다**
> (벤티지 09-23 08:18: "고치고 2회차로 바로 가고 일단 쭈르륵 이어서 계속해"). 결정은 아래 「고친 것」에 적었다.
> 근거 등급: **실측** / **코드근거** / **짐작**. 검토 중 키움·한투 조회는 한 번도 부르지 않았다. 로컬 서버도 안 띄웠다.

## 방법

1. **삼키는 자리 세기.** `catch(() => [] | null | undefined | {})` 와 빈 `catch {}` 가 서버 **599곳·137파일**, 웹 **160곳·88파일**.
   전부 읽지 않았다. 화면에 닿는 `server/src/routes/*`(44곳)를 다 읽고, 나머지는 「ok 로 기록되는 관문」에서 골랐다.
2. **「ok」의 정체.** `recordApiCall(…)` 약 100곳 중 **본문을 보기 전에 ok 를 찍는** 곳을 골랐다 — 1회차에서 네이버 PC 뉴스가
   한 달 302 였는데 ok 로 남던 유형. KIND · 네이버 토론 · 네이버 해외순위 · 야후 trending/calendar · 네이버 장중투자자.
3. **기록 대신 창구를 두드렸다.** 미니PC 의 apiUsage 는 문단속(401) 뒤라 못 읽는다(자격을 넣지 않는다). 대신
   `server/tools/silentCheck.ts` 를 새로 만들어 바깥 창구 11개를 직접 물어 **HTTP · 파싱 개수**를 쟀다(키움·한투 없음).

### silentCheck 실측 (08:41)

| 창구 | HTTP | 파싱 | 판정 |
|---|---|---|---|
| KIND 당일공시 09-23 / 09-22 | 200 / 200 | 8줄 / 100줄 | OK |
| 네이버 장중투자자(PC) `investorDealTrendTime` | **410** | 0 | 알던 것(E1-b) — 표본으로 대체 중 |
| 네이버 테마 목록(모바일 JSON) | 200 | 100 | OK |
| 네이버 해외검색 autoComplete | 200 | 4 | OK |
| 야후 trending/US | 200 | 20 | OK |
| `naverBoard(005930)` | 모듈 | 20 | OK |
| `usRank(NASDAQ, up)` | 모듈 | 20 | OK (소문자로 물으면 200 + 0줄 + ok — 라우트는 대문자화하므로 기각) |
| `futuresFlow(5)` | 모듈 | 5일 | OK |
| `fetchEtfs()` | 모듈 | 863 | OK |
| `getEtfInfo(069500)` | 모듈 | NAV 111,813 | OK |

**결론: 지금 이 순간 「ok 인데 빈」 창구는 없다.** 그러나 빈 응답이 오면 조용히 지나갈 **구조**가 여섯 군데 있었고, 그중 둘은
09-15 테마 사고와 같은 모양이다.

---

## 항목

### 1. 미국 테마·ETF 목록 — 0개를 받아도 그대로 덮어쓴다 · 우선순위 **3(조용히 빈 것)**

| 칸 | 내용 |
|---|---|
| 증상 | 네이버가 주소를 옮기거나 점검 페이지를 주면 미국 테마 136개·ETF 863개가 **0개로 바뀌고 실패 기록도 없다**(HTTP 200 → ok) |
| 자리 | `server/src/naverThemes.ts` `refreshUsThemes()` · `refreshEtfs()` — 국내 `fetchAllThemes()` 에만 09-15 가드가 있었다 |
| 근거 | **코드근거** + **선례**: 09-15 국내 테마가 정확히 이 길로 통째로 비었다(파일 머리 주석). 로컬 저장소 실측: us 136 · etf 863(09-22 16:43) |
| 재현 | 네이버 응답이 `{result:{etfItemList:[]}}` 일 때 `refreshEtfs()` → 파일의 `etf` 가 `[]` |
| 영향 | ETF 분석 메뉴·미국 테마 화면이 **한꺼번에 빈다**. 매일 받으니 하루 안에 터진다 |
| 왜 안 잡혔나 | 국내를 고칠 때 「같은 파일의 다른 두 함수」를 안 봤다 |
| 제안 → **고침** | `guardCount()` 하나로 셋이 같은 문턱(0개·절반 미만이면 던진다). 던지면 `markRun(false)` 로 실패가 남고 예전 파일은 그대로 |

### 2. KIND 백필 — 거래일의 빈 파일을 「이미 있다」로 친다 · 우선순위 **3**

| 칸 | 내용 |
|---|---|
| 증상 | KIND 가 200 으로 빈 표(점검)를 주면 `[]` 파일이 남고 ok 로 찍힌다. 그 날은 `backfill()` 이 건너뛰어 **영영 안 메꿔진다** |
| 자리 | `server/src/krxNotices.ts:148` (ok 가 파싱 전) · `backfill()` 의 `readFile → continue` |
| 근거 | **코드근거**. 로컬 실측: 09-23 파일이 새벽 01:35 에 2바이트(`[]`)로 생겼다 — 이건 무해(오늘치는 10분마다 덮어씀), 하지만 같은 파일이 **어제 날짜**로 생기면 백필이 안 본다 |
| 재현 | 어제 날짜 파일을 `[]` 로 두고 서버 재시작 → 45초 뒤 백필이 그 날을 건너뜀 |
| 영향 | 종목 상세 「시장조치」·공시 알림에서 그 날이 빈다. 거래일에 공시 0건은 없으니 빈 파일 = 실패다 |
| 왜 안 잡혔나 | 「파일이 있다」와 「받았다」를 같은 뜻으로 썼다 |
| 제안 → **고침** | 거래일 파일이 `[]` 면 없는 것으로 치고 다시 받는다 |

### 3. 네이버 장중 투자자 — 닷새 다 비면 빈 채로, 기록도 없이 · 우선순위 **3**

| 칸 | 내용 |
|---|---|
| 증상 | 410 이면 실패 기록 + 한 시간 쉼 + 우리 표본(`sampledFlow`)으로 가는데, **200 인데 표가 없으면** `{date:"", points:[]}` 를 돌려주고 아무 기록도 없다 |
| 자리 | `server/src/naverIntradayFlow.ts:199` |
| 근거 | **코드근거**. 실측은 지금 410 이라 이 길은 잠들어 있다 — 네이버가 410 을 200+빈 페이지로 바꾸는 순간 깨어난다 |
| 재현 | `fetchPage` 가 `date2` 칸 없는 HTML 을 받으면 |
| 영향 | 투자자 장중 곡선 시트가 빈 채로 뜨고 표본이 있는데도 안 쓴다 |
| 제안 → **고침** | 410 과 같은 길: 실패 기록(「닷새 모두 빈 표」) · 한 시간 쉼 · 표본 |

### 4. 시세분석 봉 — 못 받으면 칸이 조용히 사라진다 · 우선순위 **4(보기)**

| 칸 | 내용 |
|---|---|
| 증상 | 봉을 켰는데 서버가 1.2초 안에 못 받으면(또는 실패) 봉 없이 보내고, 화면은 `hasCandle=false` 로 **칸을 통째로 뺀다**. 켠 사람은 이유를 모른다 |
| 자리 | `server/src/routes/rankSpec.ts:1360-1361` (`catch(() => null)` + race 1200ms) · `web/src/pages/ScreenerPage.tsx:637` |
| 근거 | **코드근거**. 설계 의도(「한 박자 늦을 뿐」)는 맞지만 계속 실패해도 같은 모양이다 |
| 영향 | 판단엔 안 닿는다. 다만 「봉 켜기」를 눌렀는데 아무 일도 안 일어난 것처럼 보인다 |
| 제안 → **고침** | 단추가 말한다: 켰는데 이번 응답에 봉이 없으면 `📊 봉 끄기 · 아직` + 툴팁에 이유. 서버는 그대로 |

### 5. 종목 재무 — 한투 분기 실패와 「분기 없는 종목」이 같은 모양 · 우선순위 **2(판단)**

| 칸 | 내용 |
|---|---|
| 증상 | `quarterFinance().catch(() => [])` → `QuarterTable` 은 빈 배열이면 `null` 을 그린다. 그 위 **판정(FinVerdict)은 연간만으로 조용히 내려진다** |
| 자리 | `server/src/routes/news.ts:287` · `web/src/components/FinancePanel.tsx:347` |
| 근거 | **코드근거**. 주석은 「한투 키가 없거나 실패하면 분기만 빈 채로」라고 의도를 적었지만 화면엔 그 말이 없다 |
| 재현 | 한투 토큰이 만료된 순간 종목 상세 → 재무 |
| 영향 | 「지금 벌고 있나」가 작년 사업보고서만으로 판정된다. 판단에 닿는다 |
| 왜 안 잡혔나 | 「실패해도 안 죽는다」에서 멈췄다. 안 죽는 것과 말하는 것은 다르다 |
| 제안 → **고침** | 라우트가 `quartersError` 를 같이 보내고, 패널이 맨 위에 한 줄: 「분기 실적을 못 받았습니다(한투: …). 아래 판정은 연간만으로」 |

### 6. 「ok」가 파싱 앞에 찍히는 관문 다섯 · 우선순위 **3** — **안 고쳤다**

`krxNotices.ts:148` · `naverBoard.ts:148` · `usRank.ts:127` · `usMarket.ts:265,349`. HTTP 200 이면 본문이 비어도 ok 다.
**안 고친 이유:** 빈 목록이 정당한 경우가 있다(휴장일 공시 0건, 프리마켓 순위 0줄, 토론 없는 종목). 개수로 실패를 판정하면
거짓 실패가 는다. 대신 위 1·2·3 처럼 **저장·백필·대체 경로**에서 빈 것을 막는 쪽을 택했다. 기록은 「HTTP 가 됐다」는 뜻으로 두고,
`silentCheck.ts` 가 실제 개수를 재는 도구로 남는다.

---

## 기각

| 항목 | 이유 |
|---|---|
| `usRank` 소문자 거래소 → 200 + 0줄 + ok | 내 검사 실수. 라우트(`market.ts:1202`)가 `toUpperCase()` 한다 |
| KIND 09-23 파일이 새벽 01:35 에 `[]` | 오늘치는 07~22시 10분마다 다시 받아 덮어쓴다(`run(false)`). 무해 |
| `routes/*` 의 `void …catch(() => undefined)` 21곳 | 뒤에서 도는 일(텔레그램·판정·수집). 조용한 게 맞다 |
| `regularCloseOf().catch(() => null)` (`market.ts:231`) | 파일 없음 = 「정규장 중」과 같은 모양이지만 그 시간엔 어차피 안 보여 준다 |

## 미확인

- **미니PC 의 apiUsage 실제 집계** — 문단속 뒤라 못 읽었다. 1회차와 같은 유형(한 달 ok 인데 빈)이 **지금** 있는지는 창구 실측(11개 전부 OK)으로만 답했다. 열한 개 밖의 창구(DART · data.go.kr · 텔레그램 getDialogs · 야후 chart)는 안 쟀다.
- **웹 160곳** — 대부분 localStorage/설정이지만 `BriefingPage`(4) · `CisPage`(5) · `MorningPage`(4) 는 데이터 삼킴일 수 있다. 다음 회차.
- `hantooRealtime.ts:314` — 소켓 메시지마다 ok 를 찍는다. 구독은 됐는데 체결이 안 오는 경우를 못 가른다.

## 고친 것 (커밋 한 개)

| # | 결정 | 파일 |
|---|---|---|
| 1 | 미국 테마·ETF 에 국내와 같은 0개·절반 가드 (`guardCount`) | `server/src/naverThemes.ts` |
| 2 | KIND 백필: 거래일 `[]` 파일은 없는 것으로 | `server/src/krxNotices.ts` |
| 3 | 장중투자자: 닷새 다 비면 실패 기록 + 표본 | `server/src/naverIntradayFlow.ts` |
| 4 | 시세분석 봉 단추 「끄기 · 아직」 + 툴팁 | `web/src/pages/ScreenerPage.tsx` |
| 5 | 재무 `quartersError` + 패널 한 줄 | `server/src/routes/news.ts` · `web/src/api.ts` · `web/src/components/FinancePanel.tsx` |
| 도구 | `server/tools/silentCheck.ts` — 바깥 창구 11개 실측(키움·한투 없음) | 새 파일 |

눈으로 본 것: 없음(장중이라 로컬 서버를 안 띄운다). 타입 통과(server·web). 4·5 는 실패 상황에서만 보이는 글자라 정상 화면은 변화 없다.

## 다음 회차 — **(다) 손질의 순서.** `b9a92ad`(아침 되살리기) 재검증부터: 오늘 08:00 전 `rankLast` 가 어제 값으로 되살아났는지,
`worthSaving` 이 파일을 지키는지. 그 다음 **(라)+(마)**.
