# 주문 기능 설계 — 승인 대기 (2026-08-27)

> **상태: 2026-09-03 밤 구현됨 (P1 골격).** 벤티지: "바로 구현해." 아래 설계 그대로 들어갔다 —
> `server/src/orders.ts` · `server/src/routes/order.ts` · `web/src/pages/OrderPage.tsx`.
>
> **아직 한 주도 안 나갔다.** `ORDERS_ENABLED` 와 주문 앱키가 없으면 라우트가 전부 404 이고 화면은
> 「꺼져 있다」 카드만 뜬다. 첫 주문은 벤티지가 모의 앱키를 넣은 뒤다 (5절).
>
> **설계에서 달라진 것 셋** — 이유와 함께:
> ① **정정(kt10002)을 안 만들었다.** 취소하고 다시 내면 같은 결과다. 주문이 나가는 길은 적을수록 좋다.
> ② **쾌속(프리셋) 탭을 안 만들었다.** 어차피 2단계 확인을 지나야 하므로 클릭 수가 줄지 않는다.
> ③ **거래소를 골라 낼 수 있게 했다** (KRX·NXT·통합 SOR). 벤티지 요청. 거래소별로 주문 접수 시간이
>    달라서 화면이 「열림/닫힘」을 상시 보여 주고, 서버도 그 창 밖이면 거절한다.
>
> **자동주문·예약주문·스탑로스는 조사 끝에 「불가능」이 아니라 「안 한다」로 남았다** — 1절 아래 표 참고.

## 0. 확정된 결정 (2026-08-27 대화)

| 결정 | 내용 |
|---|---|
| **소액 전용 계좌만** | 주문 기능은 별도로 만든 소액 계좌에만 물린다. 큰돈은 키움 정식 앱으로 — "증권사에서 안전하게 만든 앱이 있는데 굳이 쓸 필요 없지". 모든 벽이 뚫려도 피해가 그 계좌 잔고로 한정된다 |
| **모의투자 먼저** | 키움 REST 모의투자 도메인으로 전체 기능을 만들고 몇 주 실사용 → 가드레일 검증 후 실계좌 키로 전환 |
| **자동감시주문 제외** | 서버가 혼자 주문을 쏘는 구조는 위험 등급이 다르다. 「조건 도달 → 텔레그램 알림 + 화면의 원터치 주문 버튼」까지만 — 방아쇠는 항상 사람이 당긴다 |
| **구현 신호** | 사용자가 명시적으로 시작하라고 할 때까지 착수하지 않는다 |

## 1. 왜 위험한가 — 위협 모델

지금까지 최악의 사고 = 조회 데이터 유출. 주문이 붙으면 최악의 사고 = **남이(또는 버그가) 내 돈으로 주문.**

| # | 위협 | 경로 | 대응 (아래 3절) |
|---|---|---|---|
| T1 | 외부 침입 | Cloudflare Access 우회·설정 실수 → 서버 API 도달 (서버가 키움 토큰을 쥐고 있다) | 주문 비밀번호(L1) + 킬 스위치(L5) + 알림(L6) |
| T2 | XSS | 텔레그램·뉴스·공시 등 **남이 쓴 콘텐츠**를 렌더링 → 로그인된 브라우저에서 주문 API 호출 | 주문 비밀번호(L1) + 2단계 확인(L2) + 커스텀 헤더(L7) |
| T3 | CSRF | 다른 사이트에서 인증된 세션으로 POST | 커스텀 헤더 필수 + Origin 검사(L7) |
| T4 | 내 버그 | 재시도 루프·수량 단위 착각·더블클릭 중복 제출 | 가드레일(L3) + 멱등 토큰(L2) |
| T5 | 기기 분실 | 로그인된 폰/PC | 주문 세션 TTL 10분(L1) + 소액 계좌(L0) |
| T6 | 공급망 | npm 의존성 오염 | 소액 계좌(L0) + 알림(L6) — 완전 방어는 불가, 피해 한정으로 |

## 2. 메뉴 구성 — 키움 앱 대응표

새 최상위 메뉴 **「주문」** (탭 하나, 서브탭으로 구성):

| 서브탭 | 키움 앱 대응 | 내용 |
|---|---|---|
| **매수/매도** | 키움주문 매수·매도 | 좌: 호가창(기존 컴포넌트) · 우: 주문 폼(현금만, 신용 없음). 지정가/시장가, 수량 스피너, 가능 수량 조회, 호가 클릭 → 가격 자동 입력 |
| **정정/취소** | 정정/취소 | 미체결 목록에서 줄 골라 정정·취소 |
| **미체결** | 미체결 | 실시간 갱신, 일괄취소 버튼(확인 2단계) |
| **잔고** | 잔고 | 보유 종목·평단·평가손익. 줄 클릭 → 매도 폼 프리필 |
| **체결확인** | 체결확인 | 당일 체결 내역 + 서버 주문 로그(우리가 남긴 것) 대조 표시 |
| **쾌속(프리셋)** | 쾌속주문 | 「보통 현재가 100%」 「시장가 10주」 같은 버튼 프리셋 — 프리셋 편집 가능. 단 **실행은 항상 2단계 확인을 지난다** (키움 앱의 원클릭과 달리 우리는 원클릭 즉시 발사 안 함) |

제외: 신용 주문, 대주 주문, 자동감시주문, 예약주문, 정정, 쾌속 프리셋.

### 2-1. 조사 결과 (2026-09-03) — REST 가 주는 것과 안 주는 것

| | API | 자리 | 우리 |
|---|---|---|---|
| 현금 매수 | kt10000 | `/api/dostk/ordr` | **쓴다** |
| 현금 매도 | kt10001 | `/api/dostk/ordr` | **쓴다** |
| 취소 | kt10003 | `/api/dostk/ordr` | **쓴다** (잔량 전부) |
| 정정 | kt10002 | `/api/dostk/ordr` | 안 쓴다 — 취소 후 재주문 |
| 신용 매수·매도·정정·취소 | kt10006~9 | `/api/dostk/ordr` | 안 쓴다 |
| 미체결 | ka10075 | `/api/dostk/acnt` | 쓴다 |
| 체결 | ka10076 | `/api/dostk/acnt` | 쓴다 (체결 감시도 이걸 본다) |
| 예수금 · 잔고 | kt00001 · kt00018 | `/api/dostk/acnt` | 쓴다 |
| **예약주문** | — | — | **REST 에 없다** |
| **스톱지정가** | kt10000/1 의 `trde_tp=28` | `/api/dostk/ordr` | **쓴다** — 09-04 정정, 아래 2-2 |
| **자동감시주문** | — | — | **REST 에 없다** |

아래 둘은 **영웅문 HTS 안에서 도는 기능**이다. 우리 쪽에서 흉내 내려면 서버가 조건을 지켜보다
**스스로 주문을 쏘는** 수밖에 없는데, 그게 이 설계가 0절에서 뺀 「자동감시주문」이다. 위험 등급이 다르다 —
서버가 주문을 쏠 수 있으면, 서버에 난 버그와 서버에 든 침입자도 주문을 쏠 수 있다.
대신 **「조건 도달 → 알림 → 화면에서 사람이 주문」** 이 이미 있다(손절선 감시·슈퍼신호등·급변 시그널).

보드 연계: 관심종목·슈퍼신호등·호가창 어디서든 「주문」 버튼 → 주문 탭으로 종목 프리필 (연동과 같은 길).

### 2-2. 매매구분과 스톱지정가 — 2026-09-04 정정

09-03 에 「스탑로스·스톱지정가도 REST 에 없다」고 적었는데 **틀렸다.** 키움이 8/27 에 공개한 공식
저장소(`github.com/Kiwoom-Securities/Kiwoom-REST-API`)의 `examples/국내주식/주문/buy_domestic_stock.py`
에 `trde_tp` 코드표가 있다. **18개**다 — 우리는 0·3 둘만 쓰고 있었다.

| 코드 | 이름 | 주문단가 | 조건단가 |
|---|---|---|---|
| 0 | 보통(지정가) | 필수 | — |
| 3 | 시장가 | 안 씀 | — |
| **28** | **스톱지정가** | **필수** | **필수(발동가)** |
| 5 | 조건부지정가 | 필수 | — |
| 6 · 7 | 최유리 · 최우선 | 안 씀 | — |
| 10 · 13 · 16 | 보통 · 시장가 · 최유리 (IOC) | 10만 필수 | — |
| 20 · 23 · 26 | 보통 · 시장가 · 최유리 (FOK) | 20만 필수 | — |
| 29 · 30 · 31 | 중간가 · IOC · FOK | 선택 | — |
| 61 · 62 · 81 | 장시작전 · 시간외단일가 · 장마감후 | 62만 필수 | — |

⚠️ **코드값과 이름은 공식 표지만, 「주문단가를 보내야 하나」는 추론이다.** 예제로 확인한 것은 0·3·28
뿐이다. 중간가(29~31)와 시간외(61·81)는 모의 실측에서 틀리면 `orders.ts` 의 `TRADE_TYPES` 한 줄만 고친다 —
화면은 이 표를 서버에서 받아 그리므로 따라온다.

**스톱지정가가 우리 원칙에 안 걸리는 이유.** 주문 한 번으로 발동가까지 같이 넘기므로 **우리 서버가
조건을 지켜보지 않는다.** 발동을 판단하는 쪽은 키움/거래소다. 설계가 0절에서 뺀 「자동감시주문」은
*우리 서버가* 시세를 보다 스스로 주문을 쏘는 구조를 말한다. `placeOrder` 의 유일한 호출자가
`executePrepared` 라는 성질은 그대로다.

**자를 둘 쓴다.** 손절 스톱은 현재가에서 멀리 두는 것이 정상이라 지정가와 같은 ±5% 를 물리면 기능이
죽는다. 그래서 `orderGuard.json` 에 `stopCollarPct`(기본 30)를 따로 뒀다.

* 발동가 ↔ 현재가 — `stopCollarPct` (넓게)
* 주문단가 ↔ 발동가 — `priceCollarPct` (좁게). 둘이 멀면 그건 오타다

**시간외 구분(61·62·81)은 정규장 시간창 검사를 건너뛴다.** 정규장 밖에 내는 것이 정상이기 때문이다.
시간외 창을 새로 박아 두지는 않았다 — 시간표는 2026-09-14 KRX 애프터시장 개편 때 한 번에 고치기로 한
자리라, 그때까지는 키움이 거절하게 둔다.

**손절선 감시와는 아직 안 엮었다.** 「손절선이 깨지면 자동으로 스톱주문」은 사람이 방아쇠를 당기지
않는 길이라 지금 구조에서 만들지 않는다. 손절선 알림을 보고 **화면에서 스톱주문을 거는** 것까지가 지금이다.

## 3. 보안 아키텍처 — 겹겹이 (한 겹이 뚫려도 다음 겹이 막는다)

### L0. 계좌 격리 (피해 상한)
- **소액 전용 계좌** 전용 앱키. `.env` 에 조회용 키와 **별도 키**로 저장 (`KIWOOM_ORDER_APP_KEY/SECRET`).
- 모의투자 단계에서는 모의 도메인 전용 키. 실전 전환은 `.env` 교체 + 명시적 재시작.

### L1. 주문 비밀번호 (인증 두 번째 겹)
- 별도 주문 비밀번호. 서버는 **해시만** 저장 (scrypt, salt 포함, `server/data/orderAuth.json` — git 제외).
- 입력 → 서버가 **주문 세션 토큰** 발급 (TTL 10분, 메모리 보관, 연장 없음 — 만료되면 다시 입력).
- 모든 `/api/order/*` 는 이 토큰 없이는 **404** (403 이 아니라 404 — 라우트 존재 자체를 숨긴다).
- 5회 연속 실패 시 30분 잠금 + 텔레그램 경보.

### L2. 2단계 주문 + 멱등성 (실수 방지)  ✅ 구현됨
1. `POST /api/order/prepare` — 종목·수량·가격·구분을 보내면 서버가 검증(가드레일) 후 **주문서 + 1회용 nonce** 반환 (유효 30초).
2. 화면이 주문서 내용(종목명·수량·금액·예상 수수료)을 **다시 보여주고** 확인 클릭.
3. `POST /api/order/execute` — nonce 로 실행. **nonce 는 1회용** — 더블클릭·재전송은 거부된다.

### L3. 서버 가드레일 (버그 보험 — 설정 파일로, 화면에서 못 고침)
`server/data/orderGuard.json` (기본값은 코드에, 파일은 수동 편집만):

| 항목 | 기본값 | 설명 |
|---|---|---|
| `maxOrderKrw` | 1,000,000 | 1회 주문 금액 상한 |
| `maxDailyKrw` | 3,000,000 | 1일 누적 주문 금액 상한 |
| `maxDailyCount` | 20 | 1일 주문 횟수 상한 |
| `priceCollarPct` | 5 | 지정가가 현재가 ±5% 밖이면 거부 |
| `marketHoursOnly` | true | 정규장(08:30~15:30 KST) 밖 거부 |
| `allowedCodes` | null | null=전체. 종목 화이트리스트를 걸 수도 있다 |

상한 초과는 **무조건 거부** — "경고 후 진행" 없음. 넘고 싶으면 파일을 직접 고치고 재시작.

### L4. 실행 경로 단일화
- 주문을 실제로 쏘는 함수는 서버에 **딱 하나** (`placeOrder()`), 모든 검증을 통과한 뒤에만 도달.
- 이 함수 안에서: 가드레일 재검증(방어적 이중 체크) → 키움 호출 → 결과 기록 → 알림. 다른 어떤 코드도 키움 주문 API 를 직접 부르지 않는다.

### L5. 킬 스위치
- `.env` `ORDERS_ENABLED=1` 일 때만 주문 라우터가 **등록**된다 (기본: 미등록 → 전부 404).
- 화면에도 비상 버튼: 「주문 기능 잠금」 — 누르면 서버가 메모리 플래그를 내려 재시작 전까지 전면 거부.

### L6. 감사 로그 + 즉시 알림 (마지막 방어선)
- 모든 주문 **시도**(prepare·execute·거부·실패 포함)를 `server/data/orderLog.json` 에 append-only 기록: 시각·종목·수량·가격·결과·거부 사유·요청 IP.
- execute 성공/실패는 **텔레그램 즉시 발송** (전용 갈래 또는 signal 방). 내가 안 낸 주문이 울리면 그 순간 안다.
- 체결확인 탭이 키움 체결 내역과 우리 로그를 **나란히** 보여준다 — 로그에 없는 체결 = 빨간 경고.

### L7. 웹 계층
- `/api/order/*` 는 POST 만 + 커스텀 헤더(`X-VNTG-Order`) 필수 + Origin/Referer 가 우리 도메인인지 검사 (단순 CSRF 차단).
- 주문 화면에는 남이 쓴 콘텐츠(뉴스·텔레그램)를 렌더링하지 않는다 — 주문 탭은 격리된 화면으로.
- Cloudflare Access 는 그대로 첫 겹으로 유지.

### UI 안전 장치
- 모의/실전을 **화면 전체 뱃지**로 상시 표시 (모의=파란 띠, 실전=빨간 띠). 어느 쪽인지 헷갈리는 순간이 사고다.
- 주문 폼에 오늘 남은 한도(금액·횟수) 상시 표시.
- 매수·매도 버튼 색 분리(빨강/파랑), 확인 다이얼로그에 **총액을 크게**.

## 4. 구현 로드맵 (착수 신호 후)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **P0. 실측** | 키움 모의투자 신청·모의 앱키 발급(벤티지 몫), 주문·정정·취소·미체결·잔고 TR 을 모의 도메인에서 실측 확정 (TR ID·파라미터는 추측 금지 — 이 프로젝트 원칙 그대로) | 모의로 1주 매수→체결→매도 왕복 성공 |
| **P1. 골격** | L0~L7 보안 계층 전부 + 매수/매도·미체결·잔고 탭. **가드레일·로그·알림이 기능보다 먼저 들어간다** | 상한 초과·collar 밖·시간 외·중복 nonce 가 전부 거부되는 것을 테스트로 확인 |
| **P2. 편의** | 호가 클릭 주문, 정정/취소, 쾌속 프리셋, 체결확인 대조 화면 | 모의로 2주 이상 실사용, 오발주 0건 |
| **P3. 실전 전환** | 소액 계좌 개설(벤티지 몫) → 실전 키 교체 → 가드레일 기본값 재점검 → 소액으로 시작 | — |
| (보류) | 알림+원터치(슈퍼신호등·손절선 연계), 예약주문, 스톱지정가 | P3 안정 후 별도 논의 |

## 5. 벤티지가 준비할 것 (착수 시점에)

1. 키움 **모의투자 참가 신청** + 모의투자용 앱키 발급 (키움 REST 개발자 포털)
2. (P3 때) 소액 전용 계좌 개설 — 기존 계좌와 분리
3. 주문 비밀번호 정하기 (채팅에 적지 말 것 — 첫 설정 화면에서 직접 입력)

## 6. 구현된 모습 (2026-09-03) — 설계와 대조표

| 겹 | 설계 | 들어간 것 |
|---|---|---|
| L0 | 별도 앱키 | `KIWOOM_ORDER_APP_KEY/SECRET` + `KIWOOM_ORDER_IS_MOCK`(기본 **모의**). 조회용 클라이언트와 인스턴스가 다르다 |
| L1 | 주문 세션 | 앱 아이디·비밀번호 **재입력** → 쿠키 `vntg_o`(Path=/api/order, HttpOnly, SameSite=Strict). 유휴 10분·최대 60분. 5회 실패 시 15분 + 텔레 |
| L2 | 주문 비밀번호 | 앱 비밀번호와 별개. `data/orderAuth.json` 에 scrypt 해시만. 실행마다 묻는다. 5회 실패 30분 잠금 + 텔레 |
| L3 | 가드레일 | `data/orderGuard.json` — 첫 실행 때 기본값으로 만들어진다. 한 건 100만 · 하루 300만 · 20건 · ±5% · 장중만. **넘으면 거절**(줄여서 안 낸다) |
| L4 | 단일 실행 경로 | `placeOrder()` 는 `orders.ts` 의 **비공개** 함수. 유일한 호출자는 `executePrepared`. 다른 모듈이 import 할 수 있는 주문 함수가 **없다** — 자동주문 차단은 규칙이 아니라 구조다 |
| L5 | 킬 스위치 | `.env` `ORDERS_ENABLED=1` 아니면 `/status` 말고 전부 404 · 화면의 「잠금」 버튼(풀 때만 비밀번호) |
| L6 | 기록 + 알림 | `data/orderLog.jsonl` 덧붙이기 전용(거절·실패까지). 주문·취소·체결·실패·비밀번호 사고가 전부 텔레그램 **order 방** |
| L7 | 웹 계층 | 상태를 바꾸는 요청은 POST + `X-VNTG-Order: 1` + Origin 검사. 주문 화면엔 남이 쓴 콘텐츠를 안 그린다 |

### 체결 알림 (벤티지: "체결되면 알림 오는 구조까지")

주문이 접수되면 그 **주문번호만** 감시 목록에 올린다. 5초마다 ka10076 을 보고 체결량이 늘면:

  · **알림함(종)** — 출처 `order`, 급함 urgent, 누르면 그 종목 화면으로. 설정 › 알림에서 끌 수 있다
  · **텔레그램 order 방** — `[모의]/[실전]` 딱지와 함께

다 차거나 취소·거부면 감시를 접는다. **미체결이 하나도 없으면 타이머 자체가 안 돈다** — 조회 0회다.
5시간이 지난 주문도 접는다(장이 끝났는데 도는 것을 막는다).

### 아직 안 한 것 (모의 앱키가 있어야 한다)

* 미체결(ka10075 `oso`)·체결(ka10076 `cntr`)의 **행 필드명 실측.** 지금은 아는 이름들을 후보로 훑고,
  못 찾으면 빈칸이다. 첫 응답 원문을 `orderLog.jsonl` 에 `kind:"raw"` 로 하루 한 번 남겨 두었다 — 그걸 보고 고친다.
* 예수금 필드(kt00001)도 마찬가지.
* 주문 왕복 실측(1주 매수 → 체결 → 매도).

## 7. 실전 전 개편 (2026-09-07)

벤티지: "이제 실제 계좌에서 500만원 가지고 실전 투자를 해보려고 해." 그래서 붙인 것과 잰 것.

### 7-1. 붙인 것

| 자리 | 무엇 | 어디서 오나 |
|---|---|---|
| 잔고에서 고르기 | 매수·매도 폼 위에 보유 줄 칩. 누르면 종목이 들어오고 매도면 **매매가능수량**이 채워진다. 융자 줄은 신용 매도(대출일 포함) | `kt00018 qry_tp=2`(개별) — `trde_able_qty` · `crd_tp_nm` · `crd_loan_dt` |
| 최대 | 10·25·50·**최대**. 매수는 고른 기준의 가능 수량, 매도는 매매가능수량. 한 건 한도까지만 | 아래 가능 수량 판 |
| 가능 수량 판 | **현금만** / **증거금 N%**(미수 포함, T+2) / **신용 N%**(융자) — 셋을 나란히, 하나를 고르면 그게 「최대」 | `kt00011`(증거금율별) · `kt00012`(보증금율별) · `kt20017`(신용가능여부) |
| 신용 주문 | 매수 `kt10006`, 매도 `kt10007`(`crd_deal_tp=33` + 대출일) — 자리가 `/api/dostk/crdordr` | 가드 `allowCredit` 이 true 일 때만. 모의는 거절(키움 모의는 현금만). 스톱과는 안 섞는다 |
| 잔고 탭 | 줄마다 **매수 · 매도 · 🛑 스톱** 단추 → 주소로 폼을 채운다(알림 링크와 같은 길). 융자 줄엔 배지 | — |
| 손절 칩 | 스톱지정가 매도에서 평단 대비 −3·5·7·10% → 발동가(호가 단위 내림) | — |

~~**예약주문은 여전히 없다.**~~ → 같은 날 저녁 벤티지 지시로 붙였다. **8절.**

### 7-2. 보안 점검에서 고친 것

| # | 구멍 | 고침 |
|---|---|---|
| 1 | `cf-connecting-ip` 를 무조건 믿었다. 서버가 0.0.0.0 이라 테일스케일·LAN 에서 직접 오면 헤더를 지어내 **주소별 5회 잠금을 피하고** 「처음 보는 주소」 알림을 가짜로 채울 수 있었다 | 소켓 상대가 **루프백일 때만**(cloudflared 가 같은 기계에서 붙일 때) 헤더를 믿는다 — `auth.peerIp` 하나로 |
| 2 | 주문 비밀번호·PIN 해시 비교가 `Buffer.equals`(첫 다른 바이트에서 멈춤) | `timingSafeEqual` 로 — 앱 로그인과 같은 함수 |
| 3 | 주문서(nonce)가 만든 세션에 묶여 있지 않았다 | 만든 세션만 실행. 다른 세션이 들이밀면 주문서를 태우고 기록 |

**확인했고 그대로 둔 것:** 킬스위치(`ORDERS_ENABLED`) → 앱 로그인 → 주문 세션 쿠키(`Path=/api/order` · HttpOnly · SameSite=Strict · 유휴/최대 시한) → 기기 등록(1년 쿠키, HttpOnly) → 상태 바꾸는 요청은 POST + `X-VNTG-Order` 헤더 + 같은 Origin → 주문서 30초 → 주문 비밀번호(5회 30분, 파일 기반이라 IP 로 못 피함) → 파일 한도(한 건·하루·건수·가격 울타리·장중·허용 종목·신용) → **키움에 쏘는 함수는 비공개, 호출자 하나**(`grep` 으로 다시 확인: `dostk/ordr`·`crdordr` 를 부르는 곳이 `orders.ts` 밖에 없다).

### 7-3. 실전 전 벤티지 몫

1. `.env`: `KIWOOM_ORDER_IS_MOCK=false` + 실전 앱키(500만 계좌 전용). **모의 앱키와 섞지 말 것**
2. `orderGuard.json`: `maxOrderKrw`·`maxDailyKrw` 를 계좌에 맞게(하이닉스 한 주가 174만이다). 신용을 쓰려면 `"allowCredit": true`. 첫 며칠은 `allowedCodes` 에 살 종목만 적어 두는 것을 권한다 — 손가락 실수의 범위가 그 안으로 준다
3. 주문 › 기록 탭에서 **모의 왕복이 실제로 남았는지** 확인 — `[모의] 매수 주문 … 체결`. 09-04 실측(RC9000)으로 모의 주문이 나간 것은 확인됐지만 체결 감시까지 이어진 기록은 벤티지 쪽 파일에만 있다
4. 실전 첫 주문은 **1주**로. 주문서 확인 창의 제목이 「매수」인지 「신용매수」인지 읽고 누를 것

## 8. 자동감시주문 (2026-09-07 밤)

벤티지: "어떤 종목이 얼만큼 하락하면 매수, 이런 걸 하려고 했었지. 매수 이후 얼마 이하 하락하면 매도(시장가인지, 현재가인지도 다 체크 가능하게끔). 잔고에서도 체결된 건에 대해서는 표시해 주고 관리할 수 있는 기능. 예약이 아니라 자동감시주문이 더 맞는 표현이겠다." + "예약주문 기능은 없애줘. 자동조건감시 주문을 많이 쓰지."

같은 날 저녁에 만든 **예약(08:30 에 대신 내기)은 걷어냈다** — 커밋 `1e3ed18` 에 남아 있다. 휴장일 표·거래일 판정만 자동감시가 물려받았다.

### 8-1. 이 절이 0절과 부딪히는 자리

0절은 「서버가 혼자 주문을 쏘는 구조는 위험 등급이 다르다」며 자동감시를 뺐고, 09-03 벤티지도 "자동주문은 아니어야겠지"라 했다. 09-07 밤 벤티지가 뒤집었다. **서버가 값을 보고 판단해서 내는 성질이 처음 생겼다.** 그래서 겹은 하나도 안 빼고 그 위에 올린다.

| 못 박은 것 | 어떻게 |
|---|---|
| 감시는 **주문서**다 | prepare(한도) → 확인 창(조건이 글자로) → **주문 비밀번호** → `orderWatch.json`. 만드는 길이 그것 하나(`addAutoWatch`, 호출자는 `executePrepared` 와 자식 감시뿐) |
| 키움을 부르는 함수 | `placeOrder` 는 여전히 비공개. 호출자는 `executePrepared` · `fireAutoWatch` 둘, 둘 다 `orders.ts` 안 |
| **한 번뿐** | 닿으면 한 번 내고 끝. 실패해도 재시도 없음. 같은 종목·같은 방향 감시는 하나만, 전체 20건까지 |
| 판정 값 | **KRX 체결만** — 실시간 0B(20초 안 신선한 것), 없으면 ka10095 조회(5초에 한 번 묶어서). NXT 틱은 안 본다(손절 감시와 같은 원칙) |
| 발동 창 | 정규장 **09:00~15:30**, 거래일만(휴장일 표 `KRX_HOLIDAYS`). 동시호가 예상체결가로는 발동하지 않는다 |
| 발동 순간 검문 | `allowAutoWatch` · 허용 종목 · 한 건 · 하루 합·건수(그날 이미 나간 것 포함) · 지정가 ↔ **그때 값** ±`priceCollarPct` |
| 등록 때 검문 | 이미 조건 안이면 거절(「감시가 아니라 바로 주문으로」) · 발동가가 지금 값에서 30% 넘게 멀면 거절 · 감시끼리 합이 하루 한도 안 · 유효기간 30일까지 · 전일 종가 기준은 당일만 |
| 끄기 | `orderGuard.json` `"allowAutoWatch": false` — 새로 안 받고 기다리던 것도 발동하지 않는다 |
| 기록 | 등록·취소·만료 `kind:"watch"`, 발동해서 나간 것 `"order"`(하루 한도가 센다), 검문 탈락 `"reject"`, 키움 거절 `"error"` |

### 8-2. 조건과 실행

- **기준** 값 직접 / 전일 종가 대비 % / 지금 값(등록 때) 대비 % / 평단 대비 %(매도만, 잔고의 현금 줄)
- **방향** 이하면 / 이상이면 — %면 부호가 정한다(−5% → 이하, +5% → 이상)
- **닿으면** 시장가 / 지정가(발동가) / 지정가(그때 현재가) / 지정가(직접 값, 발동가 ±collar 안)
- **유효** 당일(기본) 또는 날짜(30일까지)
- **체결되면 매도 감시 — 단계** (벤티지: "몇 프로만 팔 건지도 설정해야지"). 매수 감시에만. 단계마다 [체결가 대비 % · 체결 수량의 % · 시장가/지정가]. 1~4단계, 수량 % 합 ≤ 100(100 이면 마지막 단계가 나머지). 프리셋: −5% 전량 / −3% 반·−7% 반 / +10% 반 익절·−5% 손절 / 3단 −3/−5/−8. 체결 감시(`fillHooks`)가 부모의 체결을 알려 줄 때 단계 수만큼 자식이 태어난다(같은 `groupId`), 30일 유효. 확인 창에 단계가 글자로 적혀 같은 비밀번호로 승인된다
- **단계로 나눠 팔기** — 매도 감시 폼의 스위치. 기준(평단·지금 값·전일 종가) 대비 단계마다 감시 한 건씩(같은 `groupId`), 한 번의 확인·비밀번호. 매도 감시는 종목당 여럿 가능하되 같은 발동가는 안 되고, 수량 합이 매매가능수량을 못 넘는다
- **감시 히스토리 탭** — 지난 감시를 한 줄씩(누르면 카드), 상태 필터, 이 기록 지우기 / 모두 지우기(주문 기록엔 남는다)

### 8-3. 화면

- **자동감시 탭에 전용 폼** — 호가창 주문과 섞지 않는다(벤티지: "헷갈린다"). 종목을 고르면 `/watch/quote` 가 지금 값·전일 종가·평단·매매가능·예수금을 한 번에 준다. 매수/매도 감시 → 기준·조건·닿으면·수량(10/25/50/최대)·유효·(매수) 체결되면 매도 감시 → 「매수 감시 걸기」
- 확인 창: 「자동감시 매수하시겠습니까?」 + 조건 한 줄(`watchSay`) + 발동가 줄 + 「발동되면 총액(어림)」
- **자동감시 탭** — 맨 위가 「지켜보는 중」 **카드**(벤티지: "제일 중요한 부분"): 발동가·기준·방향 / 지금 값(실시간·조회)·발동까지 남은 % / 닿으면·수량·어림 / 유효·걸어 둔 때 + 발동가까지 얼마나 왔나 막대 + 체결 뒤 매도 감시 줄. 그 아래 「＋ 새 감시 걸기」 폼(접힘), 지난 감시 카드. 목록 응답에 `prices`(코드별 지금 값)가 실려 온다
- **잔고 탭** — 줄에 「👁 감시로 삼」 배지, 걸린 매도 감시 줄들, **잔량 띠**(보유 · 주문 중 · 👁 감시 · **남은 N주** — 남은 수가 있으면 초록), 「👁 감시매도 N주」 단추(남은 수로 폼을 채운다). 폰에서는 이름표 위·값 아래 3열(예전 2열 좌우 정렬은 숫자가 양끝으로 벌어졌다)
- **자동감시 카드** — 접힘이 기본(두 줄: 매수/매도 칩 · 종목 · 발동가↓↑ · 상태 / 지금 값 · 발동까지 % · 수량·방법 · 유효 · ↳단계), 누르면 상세·수정·취소. 「모두 펼치기/접기」
- **수정** — 카드의 「✏️ 수정」이 폼에 그 감시의 값을 채운다. 「이렇게 고치기」 → 확인 창(「기존 감시는 이것으로 바뀝니다」) → 비밀번호. 서버는 새 감시를 적는 순간 옛것을 「수정으로 대체」로 접는다(`replaceId`). **제자리에서 고치는 길은 없다** — 감시는 승인된 주문서라 고친 것도 다시 승인받는다. 옛것이 그새 발동했으면 못 접고 알린다
- 링크 `#/order?…&watch=1&wb=avg&wp=-5&wx=market` — 자동감시 탭이 열리며 폼이 채워진다
- ⚠️ 09-07 예약을 걷어낼 때 `/watch` 목록·취소 라우트가 같이 잘려 탭이 404 를 받았다. 벤티지가 잡았다 — 라우트 되살리고 `/watch/quote` 를 더했다

### 8-4. 벤티지 몫

- 모의로 한 번: 장중에 「지금 값 대비 −0.5% 시장가 매수 + 체결되면 −1% 매도 감시」 1주 → 텔레그램 「감시 발동 — 매수 나감」 → 「매도 감시 자동 등록」 → 잔고에 배지
- 휴장일 표는 2026 하반기만 — 해가 바뀌면 채울 것
- 실시간 0B 가 감시 종목에 실제로 오는지(`ensureLiveCode`) 첫 발동 기록의 「(실시간/조회)」 표시로 확인

## 9. 전체 점검 (2026-09-07 밤)

벤티지: "주문 메뉴 전체 검사. 체결/미체결은 클릭하면 반응도 느리고 미체결은 아예 화면이 안 나오네. 숫자 표시하는 UI 들이 굉장히 어색해. 자동감시나 스탑로스도 내가 추가 손댈 거 없이 그 조건에 오면 자동으로 매도가 되어야지. 모바일 모드도 철저히."

| 자리 | 찾은 것 | 고침 |
|---|---|---|
| 키움 호출 | **시한이 없었다** — 응답이 안 오면 영원히 기다려 미체결 탭이 「불러오는 중…」인 채 안 떴다 | `fetch` 에 20초 시한. 넘기면 「키움이 20초 안에 응답하지 않았다」로 실패 → 화면이 옛 줄을 남기고 다시 시도 |
| 조회 겹침 | 미체결(4초)·체결(6초)·체결 감시(5초)·잔고(10초)·감시 목록(5초)이 같은 조회를 각자 불러 주문 앱키에 초당 몇 번씩 → 429 로 밀리면 느려짐 | `memo()` 2.5초 캐시 + 겹침 합치기(미체결·체결·계좌). 화면 주기 5/8초. 앞 조회가 안 끝났으면 다음 틱 건너뜀 |
| 화면 | 실패하면 표가 비었다 | 옛 줄을 남기고 「hh:mm:ss 읽음 · N초 · 마지막 읽기 실패: 이유」 |
| 손절선 | **알림만** 보냈다 — 벤티지가 「옵션을 걸어 두면 자동으로 팔린다」고 여긴 자리 | **자동 손절**로: 값 + Enter → 「그 값 이하면 남은 수량 시장가 매도」 감시 주문서 → 확인·비밀번호 → 서버가 판다(30일). 옛 알림선은 🔔 로 보이고 「자동 손절로」 한 번에 바꿈 |
| 체결 감시 시한 | 5시간 지나면 조용히 접어 자동감시 카드가 「체결 대기」에 머물렀다 | 접을 때 자동감시에 「끝났다」고 알린다(부분 체결이면 그 수량으로 자식 감시) |
| 숫자 | 1,350,000원 / 5,000,000원 처럼 긴 숫자가 나란히 | 요약은 만원(`manwon`: 135만/500만), 정확한 값이 필요한 곳은 `<Krw>` = 1,350,000원 (135만). 띠는 라벨·값 칩, 한도 줄은 칩 나열, 단위(주·원) 붙임 |
| 폰 | 기록 표가 옆으로 잘림, 미체결/체결 카드에 같은 값 중복, 매수/매도 구분 안 됨 | 세 표 모두 `stack` 카드. 제목 줄에 색 칩 + 시각·상태를 작게, 같은 값 칸(시각·구분·상태·종류·주문번호)은 숨김, 내용은 한 줄 통째 |

**자동감시 로직 재확인(값이 조건에 오면 정말 나가나):** `runAutoWatch` 3초 → 거래일·09:00~15:30 → `livePrices`(실시간 0B 20초 안 값, 없으면 ka10095 5초 캐시) → 닿으면 `fireAutoWatch` → 검문 → `placeOrder`(시장가는 `trde_tp 3`·`ord_uv ""`) → `watch(ordNo)`+`fillHooks` → 체결되면 `onAutoWatchFill`(단계 자식). 서버가 켜져 있고 `ORDERS_ENABLED`·주문 앱키·`allowAutoWatch` 가 있으면 사람이 손댈 것 없다. 키움 스톱지정가(🛑)는 키움이 지켜본다.

## 10. 개편 (2026-09-07 밤) — "니가 나라면 어떻게 개편하라고 할래?" → "가자"

| # | 무엇 | 어떻게 |
|---|---|---|
| ① 포지션 | 단위를 주문에서 **종목 카드**로 | `/api/order/positions` 가 잔고·감시·미체결·체결·출구를 종목마다 합친다. 카드: 손익·잔량 띠·평단/지금 값/손절선/익절선·감시 카드들·미체결·오늘 체결·자동 손절 입력·매수/매도/출구 걸기. **출구 없는 카드는 빨갛다** (`noExit`). 위엔 진입 대기(매수 감시), 아래엔 보유 없는 미체결 |
| ① 출구 기본 | 매수하면 출구가 붙는다 | 주문 폼 「🛡 출구 계획」이 켜진 채로 열림(−5% 전량). 서버 `PrepareInput.exit` → 주문이 나가면 「발동된 감시」 모양의 줄을 적고 체결 갈고리 → 체결되면 단계마다 매도 감시. 끄면 확인 창에 빨간 「출구 없이 삽니다」 |
| ② 이중 손절 | 서버가 죽어도 팔린다 | 「이하면 판다」 감시(`dual` 기본 true)마다 거래일 08:31 에 키움 스톱지정가(trde_tp 28, 발동가=감시 발동가, 주문가=−1.5%)를 건다. 서버 감시가 먼저 닿으면 그 스톱을 취소하고 시장가. 키움 스톱이 먼저 팔았으면 감시를 「체결」로 접는다(`syncDualStops`). 감시 취소·수정 시 스톱도 거둔다. 스톱이 물고 있는 수량은 「감시」로 센다. 가드 `dualStop` |
| ② 심장 박동 | 조용한 죽음을 막는다 | 30초마다: 정규장에 감시 루프가 90초 넘게 안 돌았으면 🚨(10분에 한 번) · 08:55 아침 점검(감시 N건·이중 스톱 N건·실시간 상태) · 15:40 저녁 정합성(키움 체결 중 우리 기록에 없는 주문번호 → ⚠️). 포지션 탭 띠에 「감시 살아 있음/N초 멎음」 |
| ④ 탭 | 여덟 → 넷 | 주문 · 포지션 · 기록(미체결/오늘 체결/주문 기록/감시 히스토리) · ⚙ 설정. 폰은 하단 고정 바(주문/포지션/기록/⚙) |
| ⑤ 계좌 한도 | `orderGuard.json` | `maxPositionPct`(기본 40 — 한 종목이 계좌의 몇 %까지) · `maxDailyLossKrw`(기본 0=안 씀 — 오늘 자동감시 매도 실현손실이 넘으면 신규 매수 잠금, 감시 매수 발동도) · `rebuyCooldownMin`(기본 30 — 손절 발동 종목 재매수 금지) · `dualStop`(기본 true) |
| ③ 실시간 체결 | 아직 | 키움 웹소켓 주문체결 타입 실측이 먼저. 지금은 ka10076 5초 폴링(2.5초 캐시) |

**벤티지 몫:** 모의로 ① 매수 1주(출구 계획 켠 채) → 체결 → 포지션 카드에 매도 감시 자동 등록 확인 ② 다음 거래일 08:31 텔레그램 「이중 손절 — 키움 스톱 N건」(모의가 28 을 거절하면 「못 건 것 N건」이 뜨고 서버 감시만으로 간다 — 그 결과를 알려 줄 것) ③ 08:55 아침 점검 · 15:40 저녁 정합성 메시지 확인.
