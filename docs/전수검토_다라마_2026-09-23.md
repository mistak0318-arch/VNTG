# 전수검토 3·4회차 — (다) 손질의 순서 · (라) 화면의 상태 · (마) 바깥 의존 (2026-09-23 08:50~09:30)

> 의뢰서: `docs/전수검토_의뢰_2026-09-23.md`. 2회차와 같이 **실측·코드근거가 있는 것은 바로 고쳤다.**
> 근거 등급: **실측** / **코드근거** / **짐작**. 키움·한투 조회는 부르지 않았다. 로컬 서버도 안 띄웠다(장중).
> 셋을 한 문서에 둔 이유: (다)는 재검증 하나로 짧았고, (라)·(마)는 도구 하나(`silentCheck.ts`)와 `grep` 두 번으로 끝났다.

---

## (다) 손질의 순서

### 재검증 — `b9a92ad` 아침 되살리기

| 칸 | 내용 |
|---|---|
| 본 것 | `server/src/routes/rankSpec.ts:1258-1336`. 순서: `fluWasZero`(메우기 전 등락률) → 스냅샷 메우기 → `rawEmpty` 세기 → `empty` 판정 → 되살리기 / `saveLast` |
| 근거 | **코드근거**: 판정이 보는 값(`fluWasZero`·`ex.tv`)이 메우기 **전**에 잡힌다. 시장 필터(`.filter`, 1286)는 `spec.noMarket` 일 때만 도는데 판정은 `!spec.noMarket` 에서만 쓰니 분모(`drawn.length`)와 분자(`rawEmpty`)가 같은 집합이다.<br>**실측**(로컬): `server/data/rankLast/trade-value.000.3.500..json` — 어젯밤 01:37 저장본, 500줄 중 거래대금>0 **500**, 등락률≠0 **489**. `worthSaving` 이 좋은 파일만 남긴다 |
| 남은 것 | `ex.tv` 를 스냅샷 거래대금으로 **메운 뒤**(1273) `rawEmpty` 를 센다(1275). 거래대금 상위(ka10032)는 tv 가 `0` 으로 와서 `null` 이 아니라 안 걸리지만, tv 가 `null` 인 조회(거래량만 주는 TR)는 스냅샷이 메워 「빈 껍데기」를 피한다. **효과상 무해** — 그때는 등락률도 스냅샷이라 줄 전체가 어제 값으로 일관되고, 파일에도 그 일관된 줄이 남는다. 기록만 |

### 기각 — 1회차 `cd.c = cur_prc` 와 화면 실시간 덮기

칸(`cur_prc`, `ScreenerPage.tsx:1892`)과 봉(`spliceCandle`, 1506)이 **같은 `liveOf(r.code).price`** 로 덮인다. 실시간이 붙으면 둘 다 그 값, 안 붙으면 둘 다 서버 값. 어긋날 길이 없다.

---

## (라) 화면의 상태

### 목록 검증 — TODO 의 「~70곳」

`setInterval(` 은 웹 **111곳·92파일**. `useTabActive`/`usePoll` 로 문을 단 파일 **29**(+`tabActive.ts`). 나머지 63파일 중 시계·스크롤·애니메이션(MobileClock·ScrollTopButton·ExcelChrome·SysAssist·SessionBadge 등)을 빼면 서버를 부르는 자리가 대략 50. TODO 의 「~70」은 파일이 아니라 자리 기준으로 보면 맞는 자릿수다.

**주기로 줄 세워** 짧은 것부터 봤다(짧을수록 무겁다):

| 주기 | 자리 | 판정 |
|---|---|---|
| 1.5초 | `SignalBacktestPanel:140` | 기각 — `busy` 일 때만(잡 진행 폴링) |
| 2초 | `ScreenPage:197` | 기각 — 잡이 `running` 일 때만, 끝나면 `clearInterval` |
| **5초** | **`MyPage:268` `watchQuotes()`** | **항목 1** |
| 20초 | **`UsBoardPanel:698`** | **항목 2** |
| 60초 | **`InquiryRankPanel:30` · `PulsePanel:26`** | **항목 2** |
| 60초 | `HeaderTicker:70` · `SessionBadge:35` | 기각 — 머리띠라 늘 보인다 |
| 3~10분 | 나머지 시황 카드·ETF·미국 페이지 | 다음 회차 — 무겁지 않다 |

### 1. 관심종목 — 숨어서도 5초마다 키움을 부른다 · 우선순위 **2(돈에 가깝다: 조회 한도)**

| 칸 | 내용 |
|---|---|
| 증상 | 관심종목 탭을 한 번 열면 다른 메뉴로 옮겨도(`display:none`) `api.watchQuotes()` 가 5초마다 나간다 |
| 자리 | `web/src/pages/MyPage.tsx:258-273` — 문이 `document.visibilityState` 뿐 · 서버 `routes/watchlist.ts:36` — **캐시 없이** 매번 `ka10095` |
| 근거 | **코드근거**. 게이트 없는 폴링 중 **키움을 직접 부르는 가장 짧은 주기.** 기기당 시간당 720콜, 관심종목을 열어 둔 기기 셋이면 분당 36콜(4.5/s 예산의 13%) |
| 재현 | 관심종목 → 시세분석으로 옮기고 health.json `조회상위` 의 ka10095 가 5초마다 느는지 |
| 영향 | 벤티지 09-22 "탭 많이 열려있음 이렇게 될수 있는거면" 의 정확한 원인 후보 하나 |
| 왜 안 잡혔나 | `visibilityState` 검사가 있어서 「막았다」로 보였다. 브라우저 탭 기준이라 앱 안 탭엔 무력하다 |
| 제안 → **고침** | `useTabActive` 로 문 |

### 2. 시황 카드 셋 — 페이지는 시계만 막고 카드는 돌았다 · 우선순위 **3**

| 칸 | 내용 |
|---|---|
| 증상 | `OverviewPage` 는 09-22 에 `useTabActive` 를 달았지만 **자기 시계·장상태만** 막았다(271-280). 카드 `UsBoardPanel`(20초)·`InquiryRankPanel`(60초)·`PulsePanel`(60초)은 각자 `setInterval` 이라 숨어서도 돌았다 |
| 자리 | `components/overview/UsBoardPanel.tsx:695` · `InquiryRankPanel.tsx:30` · `PulsePanel.tsx:26` |
| 근거 | **코드근거**. `TabActiveContext` 는 컨텍스트라 자식이 직접 읽을 수 있다(`tabActive.ts`) |
| 영향 | 조회순위·주도주는 서버 캐시라 키움 조회는 안 늘지만 요청·렌더는 는다. 미국 전광판은 야후·한투 |
| 제안 → **고침** | 셋에 `useTabActive` 문 |

---

## (마) 바깥 의존

### 실측 — `server/tools/silentCheck.ts` 24개 + `newsCheck.ts` 5탭 (09:1x)

| 창구 | 결과 |
|---|---|
| KIND 당일공시(오늘·어제) · 네이버 테마 목록 · 해외검색 · 야후 trending · 토론(005930) · 해외순위 · 선물 수급 · ETF 목록 863 · ETF 분석(069500) | OK |
| 업비트 ticker · 야후 RSS · SEC company_tickers(10,459) · `yahooChart(^KS11)` 21봉 · `usFastQuotes` · 네이버 인기(미국) 20 · 토론 순위 98 · 예탁금 10일 · 브리핑 8 · Npay 순위 · 리서치 7 · 달력 33 · DART list.json 10 | OK |
| 네이버 장중투자자(PC) | **410** — 09-18 부터 알던 것. 표본으로 대체 중(2회차에서 200+빈 표 경로도 막음) |
| 뉴스 5탭(주요·증권·산업·글로벌·부동산·금융) 36건씩 · 쪽넘김 겹침 0 | OK |

**결론: 오늘 기준 죽은 창구는 장중투자자 하나뿐이고 이미 대체됐다.** HTML 정규식 파서(KIND `<tr>/<td>` · 뉴스 `sa_item`)도 실측 개수로 살아 있음을 확인했다.

### 안 잰 것(미확인)
- 한투·키움 — 의뢰서대로 안 불렀다.
- DART `company.json`·`fnlttSinglAcnt`(재무) — list.json 이 살아 있으니 같은 키·같은 호스트. 개별은 안 쟀다.
- data.go.kr 무역통계 — 키는 있으나 월 갱신이라 오늘 안 쟀다.
- 야후 `quoteSummary`(crumb 필요) — `usRich` 경로. 별도.
- 텔레그램 getDialogs — 세션 파일이 미니PC 에만 있다.

### 범위 밖(한 줄)
- `silentCheck.ts` 의 SEC User-Agent 는 임시 문자열이다. `usRich.ts` 의 `SEC_UA` 를 그대로 쓰는 게 맞다 — 다음에.

---

## 고친 것 (커밋 한 개)

| # | 결정 | 파일 |
|---|---|---|
| 1 | 관심종목 `watchQuotes` 5초 폴링에 `useTabActive` 문 | `web/src/pages/MyPage.tsx` |
| 2 | 시황 카드 셋에 `useTabActive` 문 | `web/src/components/overview/{UsBoardPanel,InquiryRankPanel,PulsePanel}.tsx` |
| 도구 | `silentCheck.ts` 창구 11 → 24 | `server/tools/silentCheck.ts` |

눈으로 본 것: 없음(장중). 타입 통과. 문을 단 카드는 탭으로 돌아오면 `tabActive` 가 바뀌어 effect 가 다시 돌고 즉시 한 번 받는다(`void load()` 가 effect 첫 줄).

## 회차 정리

| 회차 | 갈래 | 항목 | 기각 | 고침 | 배포 |
|---|---|---|---|---|---|
| 1 | (가) 값의 일관성 | 5 | 3 | 5 | 08:17 `32930a3` |
| 2 | (나) 조용한 실패 | 6 | 4 | 5 + 도구 | 08:29 `6e85173` |
| 3 | (다) 손질의 순서 | 재검증 1(통과) | 1 | 0 | — |
| 4 | (라)+(마) | 2 + 실측 29 | 4 | 2 + 도구 | 이 커밋 |

**의뢰서의 다섯 갈래를 다 돌았다.** 남은 것은 각 보고서의 「미확인」과, (라)의 3~10분 폴링 ~40자리(무겁지 않아 뒤로).
