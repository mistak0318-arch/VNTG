# 해외주식 상세 — 풍부하게 (2026-09-07 밤)

벤티지: "해외주식 눌렀을 때 나오는 정보들이 굉장히 적어. 국내주식에 비해서." → 시스가 프롬프트를 제안하고 "이대로 가자".

원칙: 없는 걸 있는 척 채우지 않는다. 조각마다 따로 받고 따로 실패한다(`missing`). 칸마다 출처를 적는다.

## 서브탭 (YahooChartSheet · usStock)
요약(있던 것) · 재무·실적 · 밸류·의견 · 수급 대체 · 뉴스 · 업종·동종 · 국내 연동 — `UsRichPanel.tsx`, 서버 `usRich.ts`, 라우트 `/api/us-kiwoom/rich/:symbol?summary=1`

## 출처 (09-07 실측 — NVDA)
| 조각 | 출처 | 확인한 것 |
|---|---|---|
| 키움 추가 필드 | usa20100 | 불확실성(uncert_lv)·경쟁우위(comp_adv_tp)·연중 고저·상하한가·결산월·전일 시고저·거래정지 |
| 업종 기간 수익률 | usa23000 | **stex_tp 는 숫자**(0 전체·1 NYSE·2 AMEX·3 NASDAQ), inds_cd "0". "ND" 를 주면 빈 목록. 업종명은 띄어쓰기 없이 온다(usa20100 은 있음) → 공백 제거 후 비교, 두 글자 이름은 부분 일치 금지(「IT」→「REIT」) |
| 동종 | usa23100 업종 등락률 순위 | inds_cd 로 필터됨. 상위 5 + 하위 5. (usa20550 시총 순위는 inds_cd 가 안 먹는다 — 전체 상위가 온다) |
| 재무 | SEC EDGAR company_tickers.json + companyfacts | User-Agent 에 연락처 필수. **분기 값에 누적(6·9개월)이 섞인다 — `frame` 이 CYyyyyQn 인 것만 분기.** 개념 fallback: Revenues → RevenueFromContract… → SalesRevenueNet. 영업현금흐름은 분기 단독 값이 없는 회사가 많다 |
| 밸류·의견·실적·보유·내부자·프로필 | 야후 quoteSummary v10 (fc.yahoo.com 쿠키 + getcrumb) | 모듈 10개 다 온다. crumb 는 1시간 캐시 |
| 뉴스 | 야후 RSS headline?s=TICKER | 19~20건. 한글 5줄은 generateText(설정 기본 모델), 티커·날짜별 파일 캐시 |
| 국내 연동 | usKrLinks · usKrCorrelation | 이 티커가 든 연결만 |

캐시: 키움 60초 · 업종 10분 · 동종 5분 · 야후 요약 10분 · 뉴스 30분 · 요약 24시간 · EDGAR 파일 24시간(티커 표 7일).

## 안 한 것
- 옵션(출처 없음) · 매물대(usa24220 은 종목별이 아니라 순위형) · ETF 재무(EDGAR 에 없음 — usEtfHoldings 로 구성종목)
