# 전수 검증 — 2026-09-18 (금)

벤티지 지시(07:50): "모든 기능, 아주 작은 것까지 믿고 쓸 수 있는 구조인지. 모바일·태블릿·PC. 오늘 하루 종일.
**바로 고치지 마** — 검증해서 문제·고칠 부분을 목록으로, 저녁 10시경 보고."

원칙: 읽기만 · 기록만. 주문·설정 저장 같은 **쓰는 동작은 누르지 않는다.** 소켓(kiwoomRealtime/realtimeHub/realtimeStore)은 관찰만.
접근 한계: 미니PC 는 Tailscale 공유의 `health.json`·`deploy.status` 뿐(Cloudflare Access 뒤 화면은 못 봄). 화면·로직은 **개발 PC 로컬 서버**(같은 키움 계정,
실시간·텔레그램 꺼짐)로 본다 → 실시간 의존 기능은 health 와 코드로만 판정.

## 체크포인트
| # | 시각 | 무엇 | 상태 |
|---|---|---|---|
| ⓪ | 07:50 | 착수 · 정적 감사 시작 · 905ed89 배포 대기(Tailscale) | 완료 |
| ① | 08:07 | NXT 프리 개장 | 완료 |
| ② | 09:06 | 정규장 개장 | **못 함 — 세션 한도(12:30 리셋)** |
| ③ | 10:41 | 장중 오전 | **못 함 — 세션 한도** |
| ④ | 13:34 | 장중 오후 (13:20 잠정치·↻·구성종목 실시간) | 완료 — 아래 기록 |
| ⑤ | 15:08 / 15:33 | 마감 전(종배·교차) · 마감 직후 | 완료 — ⑤-1 발견 #9(고침) · ⑤-2 공백 처리 정상 |
| ⑥ | 16:04 / 16:50 / 17:22 | 애프터 개장 VI · 15:55 회차 중간 · 회차 끝 | 완료 — 12단계 전부 ✅, 17:03 끝 (발견 #10) |
| ⑦ | 20:19 | 20:10 마무리 회차 | 완료 — **A7·A1 실측 통과** · 마무리 회차는 20:47 재확인 |
| ⑧ | 21:42 | 보고서 조립 → 22:00 보고 | 완료 — 아래 보고문 |

## 영역별 감사 (정적 + 화면)
- A. 서버 배치·스케줄러 (afterClose · collectDaily · dayMark · condAuto · liveAlerts · alertScheduler · moneyNow)
- B. 돈이 움직이는 곳 (orders · stopWatch · 자동감시주문 · 계좌·수동계좌 손익)
- C. 화면 — 전 메뉴 × 모바일 375 · 태블릿 768 · PC × 다크/라이트/엑셀/메모 (넘침·겹침·잘림·클래스 충돌)
- D. 숫자 단위·null 의미 (백만원/억/주, 0 과 「모른다」)
- E. 어제·오늘 만든 것 (해외 6 · ETF 5 · 돈의 방향 · 🤖 · VI 오늘 · 권리락 기준가 · 카드 ↻ · 구성종목 실시간 · 돈의 흐름 「지금」)

## 발견 목록
심각도: 🔴 치명(돈·데이터 손상) · 🟠 높음(틀린 값을 믿게 함) · 🟡 중간(불편·혼동) · ⚪ 낮음(다듬기)

| # | 심각도 | 영역 | 무엇 | 근거(파일:줄·실측) | 권고 |
|---|---|---|---|---|---|
| 1 | 🟡 | 운영 | 개발 PC Tailscale 이 07:4x 재부팅 뒤 「starting」에서 안 붙음 → 배포 깃발을 못 씀(905ed89 글로벌 선물 기준 수정이 미니PC 에 미반영) | `tailscale status` = NoState, `\\100.88.182.35` ping 실패 | **해결** — 벤티지 재부팅 뒤 붙음. 905ed89 08:30 배포 OK, 47c28f7(🔴 B1·B2·B16) 08:36 배포 OK · health 정상(실시간 175) |

> **08:40 벤티지 지시 변경**: "전수검증한 거 지금 다 고칠 수 있는 거면 일단 고치자" → 「기록만」에서 「확인된 결함은 고친다」로. 고친 것은 각 표의 확인 칸에 **고침(커밋)** 으로 적는다. 정책·설계 선택은 「결정 필요」로 남겨 22:00 에 묻는다.

| 2 | 🟡 | 돈의 흐름 | 「지금」 판정 띠가 **장 전(08:05) 프리마켓 수급**(외인 +410억·기관 +79억)을 +1(들어옴)로 센다 — 정규장 수조 원 단위와 같은 저울. 프로그램도 「오늘 +484억」이 장 전 값 | 로컬 08:05 실측 `flow sign=1` · moneyNow.ts buildVerdict(부호만 봄, 크기·시간대 무시) | 장 전·프리엔 flow/program 을 null(모름) 처리하거나 문턱(예: ±1,000억)을 둔다 |
| 3 | ⚪ | 시황 | 글로벌 선물 기준 수정(905ed89)은 로컬에서 정상 — 08:05 ES −0.07 · NQ −0.15 · YM −0.07 · WTI +0.05. VIX −12.82% 는 현물 어제 마감값(05:15)이라 맞음. 단 **미니PC 미배포** | 로컬 `/section/global?fresh=1` | #1 해결 뒤 배포 |
| 4 | 🟠 | 계좌 | 연동 계좌(키움) 평가가 08:48 키움 앱과 다름 — 앱 +214,694(+6.31%) vs 우리 +76,990(+2.26%). kt00018 을 KRX 로 불러 **NXT 만 열린 시간엔 어제 KRX 종가**로 잰 것 | 벤티지 캡처 08:48 · routes/account.ts | **고침(c7ae4d8)** — NXT 만 열린 시간엔 통합 현재가(ka10095)로 차이만큼 더해 다시 잼(비용 두 번 안 뺌) · 「NXT 현재가 기준」 표시 |
| 5 | 🟡 | 계좌 | 「예수금 1,509,164 vs 총자산 5,131,215, 보유 0」 — 오늘 판 돈은 D+2 정산이라 `entr` 는 그대로고 추정예탁자산만 늘어 안 맞아 보임 | 벤티지 캡처 09:44 · 로컬 실측 d2_entra=5,131,145 | **고침(c7ae4d8)** — 예수금 칸 밑에 「D+2 …」 를 붙임 |
| 6 | 🟠 | 시황 | 「선물 수급 불러오는 중…」이 영영 — 네이버 410(E1). 지수 상세 시트의 장중 수급 곡선(E1-b)도 같은 이유로 사라짐 | 벤티지 캡처 09:26·09:46 | **고침(c7ae4d8)** — 선물: 「없음 — 네이버가 닫음」 표시(대체 출처는 결정 필요). 장중 곡선: **우리 서버가 flow 섹션을 2분마다 찍은 표본**으로 다시 그림(코스피·코스닥, 배포 시각부터 쌓임 · 선물 03 은 키움에 없어 못 그림) |
| 7 | 🟠 | 시황 | 선물 수급이 키움 앱과 다름 — 우리 외국인 +42,123 vs 키움 15,789. 네이버 **새 API 값이 억원**인데(15,451 ≈ 키움 15,789) 옛 표(계약)용 「계약 × 지수 × 25만원」 환산이 그대로 걸려 2.7배 부풀음. 부호는 같았음 | 벤티지 캡처 13:35 (키움 앱 나란히) | **고침(2188200, 13:40 배포)** — 카드·선물 시트·브리핑·30일 막대·장중 곡선 전부 억원 그대로, 시장 신호등 문턱 ±2,000계약→±5,000억, 복기 밴드는 날짜별 |
| 8 | ⚪ | 데이터 출처 | 네이버 새 API 전수조사(벤티지 지시) — 죽은 것은 옛 수급 표 둘뿐, 우리가 쓰는 새 API 15개는 전부 생존. **`trend?bizdate=` 로 과거 수급을 45일까지 받을 수 있다**(실측 45/45) | `scratchpad/naver_sweep.py`·`naver_backfill.py` · 14:0x | **고침(4b57714, 14:20 배포)** — 선물 수급 소급 채우기(실측 30일 · 8/07~9/18 · 미니PC 도 같은 코드라 첫 호출에 채워짐). 결과 표는 docs/다음작업_TODO.md 에. ETF 목록 폴백만 남김 |
| 9 | 🟠 | 돈의 흐름 | **「마감 전」 슬롯의 종배 후보·14:30 교차가 영영 안 뜬다** — `buildPlan` 이 `scan.date === date` 를 요구하는데, 종배 스캔은 9/16 에 **18:30 뒤 예비 경로**로 바뀌었다(파이프라인 15:55 가 본길). 즉 14:30~15:20 에는 오늘 스캔이 있을 수 없어 늘 「오늘 스캔이 아직 없습니다」 + 교차 빈 배열. 어제 만든 교차 기능이 **제 시간대에 한 번도 못 뜬다** | moneyNow.ts buildPlan :934-948 (`scan.date === date`) · closeBetScan.ts tick `min < 18*60+30` · 로컬 15:08 실측 cross=[] | **고침(de53467, 15:20 깃발)** — 벤티지 ①선택: 어제 원장으로 내고 기준일 표시(5거래일 넘게 묵으면 안 씀). 로컬 실측 교차 3건(GS건설·대한광통신·HD현대, 09/15 기준) |
| 10 | 🟡 | 배치 | **`condAuto` 「건너뜀 1」의 사유가 health 에 안 실린다** — 「마크가 오늘 것이 아니라 안 돌림」(조건식 자동 전체가 죽은 것)과 「식 하나가 신호등 기준을 써서 손으로 돌려야 함」(정상)이 **같은 문구**로 보인다. 9/17·9/18 둘 다 「0식 돌림 · 건너뜀 1」이라 어느 쪽인지 원격으로 못 가린다 | condAuto.ts:78 vs :87 (둘 다 skipped 1) · afterClose.ts:696 은 개수만 적음 | **고침(cba9cd3, 17:43 배포)** — 사유 첫 줄을 메모에 함께 적는다 |
| 11 | 🟠 | 배치 | **오늘 고친 A3 가 과잉 교정** — 「200 인데 빈 응답은 실패」를 **모든 종류에 똑같이** 걸었다. 그런데 `short`(공매도)·`loan`(대차)는 **그 종목에 공매도·대차가 없으면 빈 게 정상**이다. 20:10 ledgerFinal 이 **실패 137**(어제 0) — 그 137 종목은 도장이 안 찍혀 **매 회차 영영 다시 받고**, 실패 숫자가 늘 137 이라 「진짜 실패」를 가리는 소음이 된다 | collectDaily.ts:380 `if (got.length === 0) throw` · health 20:47 ledgerFinal 실패 137 vs 9/17 실패 0 · 15:55 ledger(flow·fgnRatio·prog)는 실패 2 로 정상 | 빈 응답을 **flow·fgnRatio·prog 에만** 실패로 센다(그 셋은 종목마다 날마다 있어야 하는 값). short·loan 은 빈 것을 정상으로 보고 도장을 찍는다. **22:30 배포 금지 풀리면 고침** |

### A. 서버 배치·스케줄러 (정적 감사, 08:40 완료 · A1·A2·A5·A10 은 시스가 재확인)
| # | 심각도 | 무엇 | 근거 | 확인 |
|---|---|---|---|---|
| A1 | 🟠 | **20:10 이 지나면 정규 회차·재시도가 영영 못 돈다** — `shouldWrap()` 블록의 모든 갈래가 return (wrapDone 이면 :930 에서 return). 미니PC 가 20:30 에 켜진 날, 또는 15:55 회차의 실패 단계 재시도가 20:10 을 넘기면 원장·편입·마크·조건식이 그날 안 돈다(편입 원장은 소급 불가) | afterClose.ts:925-953 | **재확인** |
| A2 | 🟠 | 장중에 「재수집」을 누르면 **오늘 줄은 버리면서 fetchedAt 은 찍어** 15:55 회차가 그 종목들을 건너뜀 → flow/prog 는 다음 날 메워지지만 **외국인 지분율(fgnRatio)은 그날 치가 영구 손실** | collectDaily.ts:119-124 · :145-155 · :389 | **재확인** |
| A3 | 🟠 | 키움이 200 + 빈/오류 봉투를 주면 **성공으로 집계**(fetchedAt 찍힘, 실패 수 안 늘음) → 「✅ 원장 2,600/2,600」인데 빈 날 | collectDaily.ts:382-392 | 에이전트 확인 |
| A4 | 🟠 | 일봉 단계 — 전 종목 조회가 실패해도 `builtAt`=오늘, 메모는 **저장돼 있던 종목 수**를 세서 「✅ 일봉 2,845종목」 → 마크·표본·장세가 어제 봉으로 초록 보고, 재시도도 막힘 | dailyCloses.ts:537-546 · :559 · :522 | 에이전트 확인 |
| A5 | 🟠 | 「지금」 판정 기록이 **둘째 날부터 멈춘다** — `lastRecordedMin` 을 날짜 넘겨 비교(545−930<10 → return). 배포 없는 날엔 추세·성적표가 영영 안 쌓임 (어제 내가 넣은 것) | moneyNow.ts recordVerdict | **재확인** |
| A6 | 🟠 | ⑦-2 전종목 마크 단계가 실패를 **문자열로 돌려줘 ok:true** — 같은 파일 주석이 경고한 바로 그 함정. 실패해도 ⚠️ 없음·재시도 없음, ⑧-3 조건식 자동이 옛 마크로 돈다 | afterClose.ts:673 · stockMarks.ts:362-366 | 에이전트 확인 |
| A7 | 🟠 | **일봉 c/ca 분리가 하루만 산다** — 다음 날 병합이 어제 줄을 통째로 덮어 c 가 애프터 포함 종가로 바뀌고 ca 가 사라짐. 「c = 정규장 종가」 불변식(표본·검증표·백테스트의 전제)이 깨짐 | dailyCloses.ts:289-301 | 에이전트 확인 — **오늘 20:10 뒤 어제(9/17) 봉으로 실측 예정** |
| A8 | 🟠 | 종배 스캔이 통째로 실패해도 **빈 결과를 오늘 것으로 저장** → 재시도 막힘, 「후보 없음」과 「스캔 실패」 구별 불가(「지금」 플랜·14:30 교차도 그걸 읽음) | closeBetScan.ts:139-179 · :212 | 에이전트 확인 |
| A9 | 🟡 | 돈의 흐름 브리핑 — **보내기 전에 도장**(markToday) → 잔고 오류·텔레그램 429 한 번이면 그 회차는 그날 건너뜀 (어제 내가 넣은 것) | moneyNowBriefing.ts:25-26 | **재확인** |
| A10 | 🟡 | afterClose `dayKey()` 가 TZ=Asia/Seoul 에선 **UTC 날짜** — 09시 전에 손으로 돌리면 어제 상태를 덮고 health 요약이 틀림 (다른 모듈은 +9h 방식으로 맞음) | afterClose.ts:353-360 | 에이전트 확인 |
| A11 | 🟡 | 실시간 알림(VI·체결강도) 중복 키를 **보내기 전에 저장** — 텔레그램 실패 시 그 알림은 유실 | liveAlerts.ts:200-203 · :275 · :312 | 에이전트 확인 |
| A12 | 🟡 | alertScheduler `isMarketHours` 가 **주말만 거르고 휴장일을 모름** — 추석(9/24 목)에 손절 감시·실시간 알림·시그널 스캔이 전날 값으로 돈다 (9/10 점검이 쓸었다던 그 부류) | alertScheduler.ts:58-66 | 에이전트 확인 |
| A13 | 🟡 | 「🧲 조용히 담는 중」이 체결강도 **모름(null)을 통과로** — 순위판 종목은 전부 null 이라 「강도 105↑」 문구가 거짓 (어제 내가 넣은 것) | moneyNow.ts buckets.quiet | **재확인** |
| A14 | 🟡 | 마크 계산이 모르는 시총·거래대금을 **0 으로** computeAlerts 에 넘김 → getSharesMap 한 번 실패하면 쏠림·탈락이 0 기준으로 찍히고 조건검색이 사실로 씀 | stockMarks.ts:310-336 | 에이전트 확인 |
| A15 | 🟡 | 「지금」 60초 캐시에 **최대 나이 없음** — compute 가 계속 실패하면 어제 15시 판정을 stale 표시만 달고 계속 줌(브리핑도) | moneyNow.ts moneyNow() | **재확인** |
| A16 | 🟡 | `etfSentiment` 에 in-flight 잠금 없음(etfFlow 엔 있음) — 동시 호출이 전체시세 15쪽을 중복 조회 | etfFlow.ts:269-301 | 에이전트 확인 |
| A17 | 🟡 | etfFlow `marketOpened()` 가 휴장일을 모름 — 한글날(10/9) 10시에 asOf=오늘·전 종목 0% | etfFlow.ts:134-138 | 에이전트 확인 |
| A18 | 🟡 | dayMark 가지치기가 날짜 꼬리 없는 `once()` 키를 재시작 때마다 지움 → 배포마다 알림 재발 가능(naverAlerts 의 g.date 가 ISO 가 아니면 해당) | dayMark.ts:42-44 | 에이전트 확인 |
| A19 | 🟡 | `buildCloses` 가 실행 중이면 `force` 를 무시 — 20:08 에 화면이 부른 비강제 실행 결과를 20:10 barsFinal 이 받아 「✅」 찍고 wrapDone | dailyCloses.ts:442-443 | 그럴듯(창 좁음) |
| A20 | 🟡 | 마크 파일 `day` = 종목 중 **가장 늦은** 봉 날짜 → 일봉이 대부분 실패해도(A4) 「오늘」로 찍혀 조건식 자동이 신선도 검사를 통과 | stockMarks.ts:237 · :344 | 에이전트 확인 |
| A21 | 🟡 | 뒤집힘 알림이 09:00~15:30 만(스케줄러는 20:00 까지 돈다) — 애프터의 조용→빠짐은 안 울림. 또 하루 2회 카운트를 **보내기 전에** 올림 (어제 내가 넣은 것) | moneyNow.ts flipAlerts | **재확인** |
| A22 | ⚪ | 내 계좌 렌즈 — 수익률 미수신이 **0.00%** 로, 평가액 5천 원 미만은 「모름」으로 (null 규칙 뒤집힘) | moneyNow.ts buildAccount num() | **재확인** |
| A23 | ⚪ | 글로벌 빠른 갱신 키에 `jpyusd` 오타(실제 키 `usdjpy`) → 달러/엔만 10초가 아니라 60초 주기 | globalMarket.ts:288 vs :177 | 에이전트 확인 |
| A24 | ⚪ | NXT 프리 08:50 마감이 코드 어디에도 없음 — 08:50~09:00 도 「프리」 | marketHours.ts:49-57 · moneyNow slotOf | 그럴듯 |
- 공통: `catch → []/0` 뒤 그 빈 값을 **저장하고 사실로 쓰는** 곳이 A2·A3·A4·A8. 「실패를 성공으로 굳히지 않는다」는 이 도구의 제1원칙이 배치 쪽에 넷 남아 있다.

### B. 돈이 움직이는 곳 (정적 감사, 08:30 완료 · 🔴 둘은 시스가 코드로 재확인)
| # | 심각도 | 무엇 | 근거 | 확인 |
|---|---|---|---|---|
| B1 | 🔴 | **매매가능수량 0 을 총보유로 되돌린다** — `ableQty: able > 0 ? able : qty`. 0 의 정상 뜻은 「낸 매도(손절 스톱)가 전부 물고 있다」인데 상류에서 덮여, 아래 가드 넷이 죽는다: 매도 수량 검사(:1517-1530 `ableRaw` 가 0 이 될 수 없음) · 감시 발동 시 「매도 가능 0 → 보류」(:3119/:3169) · 포지션 pendingQty/freeQty(:3582/:3600) · 매도 감시 등록 상한(:1356). 시나리오: 100주에 이중 스톱 100주 → 「전량 매도」가 통과 → 스톱 100 + 시장가 100 이 같이 걸림 | server/src/orders.ts:2215 (9/8 검진 8 주석 :1507-1514 과 정면 충돌) | **재확인** |
| B2 | 🔴 | **감시주문이 15:30~16:00 공백에 발동되고, 거절되면 손절이 영영 사라진다** — 주석(:2484)은 「발동해도 venueOpen 이 막는다」지만 `venueOpen` 은 :1376(일반 주문)·:4214(상태)에서만 불린다. `fireAutoWatch` 엔 없음. 15:30 종가 단일가가 발동가를 넘기면 닫힌 KRX 로 나가 거절 → `failed` 는 종결 상태(되살리는 코드 없음) → 애프터 4시간 + 이후 매일 손절 없음, 화면엔 stopLine 이 그대로 | orders.ts:2927(창) · :2947-3216(fire) · :3210(failed) | **재확인**(호출부 grep) |
| B3 | 🟠 | 주문 가격 보호대·시장가 금액(하루 한도)이 **통합(_AL) 가격** 기준 — NXT 얇은 체결에 흔들림. 프로젝트 원칙(:2756 「분석·판정은 KRX」)과 어긋남 | orders.ts:1425 → cisRun.ts:88 | 에이전트 확인 |
| B4 | 🟠 | 감시 발동가 계산은 _AL, 발동 판정은 KRX — 기준이 다른 두 시계열 (애프터에 건 「지금 값 −3%」가 다음 날 2% 일찍 발동) | orders.ts:2505 · :1267-1290 · :2767 | 에이전트 확인 |
| B5 | 🟠 | 하루 금액·건수 한도가 **로그 꼬리 8,000줄**에서만 센다 — 바쁜 날 아침 매수가 꼬리 밖으로 밀리면 한도가 조용히 풀림. 실행 직전 재확인도 같은 창 | orders.ts:640-655 · :1490 · :1809 | 에이전트 확인 |
| B6 | 🟠 | `maxPositionPct` 를 **40 으로 저장하면 읽을 때마다 0 으로** 바뀜(옛 기본값 마이그레이션이 매번 돈다) → 종목 집중 한도 미적용, 40% 를 표현할 길 없음 | orders.ts:524 | **재확인** |
| B7 | 🟠 | 아침 이중 스톱을 **원래 수량 그대로** 다시 검 — 일부 매도한 뒤엔 거절되고 그날 재시도 없음(`dualDate` 찍힘) → 하루 종일 거래소 스톱 없이, 화면엔 있는 것처럼 | orders.ts:3328-3371 · :2874-2894 | 에이전트 확인 |
| B8 | 🟡 | 손절선 감시(stopWatch)가 **나이 검사 없이** 마지막 틱을 「지금 값」으로 — 소켓이 끊긴 뒤의 옛 값으로 거짓 이탈 알림, 그날 진짜 이탈은 중복 키로 묻힘 | stopWatch.ts:69-82 (orders.ts:2788 은 20초 가드 있음) | 에이전트 확인 |
| B9 | 🟡 | 포지션 손익이 한 화면에 **세 가지 셈법** — 줄은 (실시간가−평단)×수량, 머리는 kt00018 현재가, 키움 평가손익(비용 포함)은 안 씀 → 줄 합 ≠ 머리, 둘 다 키움 앱보다 좋게 보임 | OrderPage.tsx:3902 · orders.ts:3613 · :2218 | 에이전트 확인 |
| B10 | 🟡 | 감시주문 폼(WatchForm)이 종목을 바꿔도 **앞 종목의 발동가·지정가·수량**을 들고 있음(OrderForm 은 9/8 에 고침). 30% 안이면 서버도 통과 | OrderPage.tsx:2720-2739 | 에이전트 확인 |
| B11 | 🟡 | 청산 계획(exit·then)의 0주 단계가 **말없이 빠짐** — 1주 매수에 2단계 계획이면 −3% 단계가 사라짐(legs 경로만 미리 거절) | orders.ts:3250 vs :1319 | 에이전트 확인 |
| B12 | 🟡 | 손절을 **없애는** 동작(감시 취소·삭제·stop:0)은 주문 비밀번호 없이 세션만으로 | routes/order.ts:479 · :463 · :409 | 에이전트 확인 |
| B13 | 🟡 | 이중 스톱 체결량을 **합이 아니라 최댓값**으로 — 40+40 두 번 체결이면 40 으로 읽고 60주를 또 매도할 수 있음. ka10076 cntr_qty 가 누적인지 실체결로 확인 필요 | orders.ts:3384-3388 · :3866 | 그럴듯 |
| B14 | 🟡 | 손으로 친 주문단가·정정단가는 **호가 단위로 안 맞춤**(서버가 만든 값은 전부 toTick) → 마지막 단계에서 키움 거절, 「5분 3회 거절」 경보에도 셈 | orders.ts:1206 · :1657 · OrderPage.tsx:2193 | 에이전트 확인 |
| B15 | 🟡 | 일일 손실 차단기가 **익절과 손절을 상계**하고 비용 무시 — 손절 −90만 뒤 익절 +85만이면 차단 풀림 | orders.ts:1586-1597 | 에이전트 확인 |
| B16 | 🟡 | `inCloseAuction` 이 15:20:00~15:20:59 를 빼먹음(`>` 920) — 그 1분에 발동하면 시장가가 동시호가로 | orders.ts:3018 | **재확인** |
| B17 | 🟡 | `failed` 는 종결 — 예상 못 한 키움 거절 한 번이면 손절 영구 소멸(거래정지·호가단위·관리종목 애프터 등). 매도 감시는 N회 재시도가 안전 | orders.ts:3064-3071 · :3210 | 에이전트 확인 |
| B18 | 🟡 | 수동 계좌 — 시세 조회가 실패한 종목은 당일 손익에서 **말없이 빠짐**(「못 받음」 표시 없음) | ManualAccountPage.tsx:89-91 · manualAccounts.ts:354 | 에이전트 확인 |
| B19 | 🟡 | 수동 계좌 예수금이 보유 수정 때마다 원가 차이만큼 이동 — 타임아웃 뒤 재시도가 겹치면 예수금이 어긋나고 흔적 없음 | manualAccounts.ts:232-249 | 에이전트 확인 |
| B20 | ⚪ | 원장 비용 「measured」가 혼합 수수료율을 양쪽에 다시 곱함(과대, 의도적일 수 있음) | orderLedger.ts:335-348 | 에이전트 확인 |
| B21 | ⚪ | 손절 이탈 알림의 `lossPct` 가 부호 있는 수익률 — 올려 둔 손절선이 깨지면 「이탈 (+2.1%)」 | stopWatch.ts:182 | 에이전트 확인 |
| B22 | ⚪ | 키움 원응답(예수금·보유 전체)이 orderLog.jsonl 에 평문·무회전으로 쌓임. **지금 새는 곳은 없음**(라우트가 raw 를 거름, health.json 깨끗) | orders.ts:2044 · routes/order.ts:491 | 에이전트 확인 |
| B23 | 🟡 | 시세 조회가 실패하면(ref=0) 지정가 보호대가 **통째로 꺼짐** — 0 하나 더 친 값이 그대로 나감 | orders.ts:1423-1464 | 에이전트 확인 |
| B24 | ⚪ | 비밀번호 틀리면 같은 주문서로 재시도 허용(의도·문서화됨, 5회/30분 잠금이 상한) | orders.ts:1778 | 에이전트 확인 |
| B25 | ⚪ | 휴장일 표가 2027-01-01 까지 하드코딩 — 빠진 휴장일 하루에 감시가 전부 `failed` 될 수 있음(B17 과 결합) | orders.ts:2395-2402 | 에이전트 확인 |
- 깨끗했던 것: nonce 이중 실행 방지 · 재시작 재생(fired 선기록) · health.json 에 계좌·잔고·종목·키 없음 · 호가 단위 표(2023-01) · 모의투자는 실주문 경로 없음.

### C. CSS — 이름 충돌·새는 규칙·모바일 넘침·테마 빠짐 (정적 감사, 09:00 완료 · C1~C3 은 시스가 grep 재확인)
| # | 심각도 | 무엇 | 근거 | 어디서 깨지나 |
|---|---|---|---|---|
| C1 | 🟠 | **`.pulse` 가 두 화면에서 따로 정의** — 시황 「주도주·급소」(PulsePanel)와 「지금의 화제」(TopicPulse). 둘 다 스코프 없이 합쳐져: 주도주 카드 안에 테두리·배경·여백 카드가 한 겹 더, 화제 블록은 flex 열로 바뀌어 간격이 겹침 | styles.css:18606 vs keywordFlow.css:652 (어제 `mn-card` 와 같은 모양, **지금 살아 있음**) | 전 폭·전 테마 |
| C2 | 🟠 | `.sc-card` — 시나리오 카드 모달(max-width 420)과 현미경 섹션이 같은 이름 → 현미경 카드가 **420px 에 묶임**(1100px↑ 에서 칸마다 빈 띠), 모달엔 배경·테두리·overflow 가 덧씌워짐 | styles.css:7542 vs :16962 | PC ≥1100 |
| C3 | 🟠 | `.sc-grid` — 시나리오 카드 입력 폼(2열)이 현미경 규칙(1열/2열/3열)에 덮여 **폰·노트북에서 1열로 쌓이고** 1700px↑ 에선 420px 모달에 3열 | styles.css:7546 vs :16959-16961 | 전 폭 |
| C4 | 🟡 | `.sc-note` — textarea 가 `display:flex` 를 받음(현미경 래퍼 규칙) | styles.css:7564 vs :16957 | 전 폭 |
| C5 | 🟡 | `.sc-head`·`.sc-hint` 도 같은 두 화면이 공유 → 현미경 페이지 머리가 space-between 으로 | styles.css:7543,7545 · ScopePage.tsx:606 | 전 폭 |
| C6 | 🟡 | `.pulse-foot`·`.pulse-term`·`.pulse-ev-q` — keywordFlow.css 가 나중에 로드돼 주도주 카드 꼬리말이 **px 글씨**(글자 크기 설정 안 먹음)+여백 9px | styles.css:18621,13780,13803 vs keywordFlow.css:757,690,749 | 전 폭 |
| C7 | 🟡 | `.usm-sub`(업종 MAP 용)를 **여섯 화면**이 범용 부제목으로 씀 — 한쪽 손질이 다섯 곳에 번짐. 옛 `usm-` 블록(UsBoardPanel)도 따로 있음 (어제 내가 만든 것) | styles.css:19325 · :7395-7406 | 다음 사고 후보 |
| C8 | 🟡 | `.ubz-row` 4열 격자에 인기·화제 페이지는 자식 3개 — 등락률이 오른쪽 끝에 안 붙고 ETF 순위와 줄이 어긋남 | styles.css:19330 · UsBuzzPage.tsx:71-113 | 전 폭 |
| C9 | 🟡 | `.sig-checks` 두 번 정의 — 둘째가 첫째를 **순서로만** 누름. 블록을 옮기면 700~899px 태블릿 회귀 | styles.css:3539-3540 vs :15123-15125 | 태블릿(잠복) |
| C10 | 🟡 | `.ord-side` grid vs inline — 머리는 스코프로 막았지만 `.ord-side button` 셋은 무스코프 | styles.css:15360,17062 · :15369,15693,17461 | 주문(잠복) |
| C11 | ⚪ | `.ord-stop-in` 폭 5.6rem vs 9.5rem — 잔고 줄 손절 칸이 설계보다 70% 넓음 | styles.css:15969 vs :17285 | 주문 |
| C12 | ⚪ | `.map-grid` 기본 선언 둘(px 쪽은 죽은 코드) · `.ord-bottombar`/`.ord-subs` 는 `!important` 로 죽인 블록이 남음 | styles.css:963 vs :10399 · :17348-17411 | — |
| C13 | 🟠잠복 | 후손 `span`/`div` 에 레이아웃을 거는 규칙 — `.ns-row span`·`.rts-nums span`(모든 깊이의 span 이 세로 flex) · `.sc-kv div`·`.ia-kv div`·`.peek-kv div`·`.mrn-sup div` | styles.css:18019,12033 · :16965,16988,17011,5531 | 배지·점 하나 넣는 순간 |
| C14 | 🟡 | 새 블록의 맨 요소 규칙 — `.ets-col i`(9px 막대 강제)·`.ueh-bar i`·`.mfp-nm i`·`.ubz-row em`·`.uss-row > span|i` (어제 내가 만든 것) | styles.css:19366,19380,19399,19338,19352 | 잠복 |
| C15 | 🟡 | `.data-table td { overflow:hidden }` + sticky th — 셀 안 팝오버·툴팁이 잘림 | styles.css:11054 · :1635 | 잠복 |
| C16 | 🟡 | 컨테이너 안 모든 `button` 을 격자·전폭으로 — `.wb-list`·`.bd-rank`·`.pt-results`·**`.ov-seg`(네 페이지 여섯 곳)**·`.ord-bottombar` | styles.css:11736,17717,6299 · overview.css:401 | 잠복 |
| C17 | 🟠잠복 | `.mnw-row > em.num` 과 `.mnw-row > em.mnw-pnl` 이 **같은 우선순위** — 어제 겹침을 순서로만 고쳤다. 재정렬·압축기 한 번이면 재발. `.num` 은 전역 26규칙짜리라 격자 열쇠로 부적합 (어제 내가 고친 것) | styles.css:19468 vs :19481 | 「지금」 계좌 줄 |
| C18 | 🟡 | `.mfp-nm` 이 nowrap 인데 제 몸에 overflow 가 없음 — 긴 ETF 이름이 등락·배수 칸 위로 (≤430px) | styles.css:19398 | 폰 |
| C19 | 🟡 | `.mfp-cols` 최소 270px — **320px 폰(SE·폴드 커버)** 에선 내용 폭 262px 라 페이지 가로 스크롤. `.mnw-where`(240px)도 같은 모양 | styles.css:19392 · :19451 | 폰 320 |
| C20 | 🟡 | `.nvbb-pop` 이 폰용 `.ht-pop` 규칙(좌우 꽉 채움)을 순서로 덮음 — 481~720px 에선 460px 로 왼쪽에 붙음, 블록 옮기면 어제 잘림 재발 | styles.css:19417 vs :7383-7390 | 폰·작은 태블릿 |
| C21 | 🟠 | **메모 테마에 빠진 135개 클래스** — 엑셀 테마는 리터럴 색을 `!important` 로 덮는데 메모엔 없음 → 현미경의 빨강 테두리(#ef4444)·노랑 칸이 크림색 종이 위에 그대로. 위장 테마의 목적이 깨짐. 라이트 테마도 같은 리터럴이 그대로 | note.css vs excel.css:1031-1069 · styles.css:16919-16951 | 메모·라이트 |
| C22 | 🟡 | `.nvbb-new` 흰 글씨+`var(--red)` — 메모·엑셀에선 **검은 알약에 흰 글씨**가 머리글에 (어제 내가 만든 것). `.ord-noexit`·`.ord-wchip`·`.cis-stamp.bad` 도 | styles.css:19415 · :17319,17204,17776 | 메모·엑셀 |
| C23 | 🟡 | 리터럴 hex 다섯 — 해외 상세 의견 막대(`.usr-trend-bar`)·`.live-dot.wait`·`.msig-early` 는 어느 테마에서도 안 바뀜 | styles.css:17602-17606 · :16511 · :17763 | 전 테마 |
| C24 | 🟡 | `--ov-*` 변수는 `.ov,.ib` 안에서만 정의 — 밖에서 쓰는 `ov-` 클래스가 변수를 못 받음: **ETF 자금흐름 두 페이지의 1일/5일/20일 탭(.ov-seg)이 알약 배경 없이 굵기로만 구분** · 데일리 리포트 `.ov-li` 줄 구분선·호버 사라짐 · 「지금」의 ↻ 색 (어제 내가 만든 것) | overview.css:17-28 · EtfFlowPage.tsx:66 · UsEtfFlowPage.tsx:61 · ReportSections.tsx:555-813 | 전 폭·전 테마 |
| C25 | ⚪ | px 글씨가 통째로 남은 파일 — keywordFlow.css 39/44 · login.css 35/35 · excel.css 16/17 → 글자 크기 설정이 그 화면들에만 안 먹음 | 세 파일 | 큰 글씨 사용자 |

### D. 웹 — 틀린·묵은 숫자 (정적 감사, 08:20 완료 · 경로는 web/src 기준)
| # | 심각도 | 무엇 | 근거 | 확인 |
|---|---|---|---|---|
| D1 | 🟠 | 테마/업종 MAP 의 「기준 시각」이 **다른 데이터의 시각** — 내 태그·네이버·관심종목 모드에서도 업종 섹션 시각을 보여 주고, 네이버 테마 타일은 **한 번만** 받고 다시 안 받는다(탭을 켜 두면 09:05 지도에 10:32 시각) | pages/MapPage.tsx:295 · :129-136 | 확인 |
| D2 | 🟠 | PriceHeader 가 종목이 바뀔 때 **앞 종목 상태를 안 지운다** — NXT·KRX 고저·회전율·상장주식수·체결강도가 한 박자 앞 종목 것. 새벽 rolled 경로에선 앞 종목 일봉이 **큰 숫자**가 될 수 있다 | components/PriceHeader.tsx:118-162 · :303-331 | 확인 |
| D3 | 🟠 | 개별종목분석에서 관심종목 추가 시 편입가가 **부호 붙은 cur_prc** 그대로 — 내린 종목은 **음수 편입가**, info 없으면 0 | pages/StockAnalysisPage.tsx:229 (다른 곳은 Math.abs) | 확인 |
| D4 | 🟠 | 구성종목 시트(네이버·us·etf 테마) — 모르는 등락률이 **0.00%** 로 | components/overview/ConstituentSheet.tsx:133 → 240 | 확인 |
| D5 | 🟡 | 같은 시트 — 모르는 전일대비가 0 (가드가 price 에만) | ConstituentSheet.tsx:132 → 238 | 확인 |
| D6 | 🟡 | 글로벌 카드 — change 는 있고 rate 가 null 이면 `(+0.00%)` | pages/OverviewPage.tsx:586,591 | 확인 |
| D7 | 🟡 | 관심종목 「실시간 ●」이 **REST 폴백 값에도 켜진다** (밤·주말·소켓 죽었을 때도 ●, title 은 "키움 실시간 1.5초") | pages/MyPage.tsx:273-287 · :1143 | 확인 |
| D8 | 🟡 | 알림종 — 필터를 빨리 바꾸면 늦은 응답이 덮어써 목록·안 읽음 수가 어긋남(요청 순서 가드 없음) | components/NotifyBell.tsx:400-424 | 확인 |
| D9 | 🟡 | 구성종목 시트 실시간 창(`krxOverlayLive`)이 **시트를 연 순간에 얼어붙음** — 07:58 에 열면 08:00 넘어도 안 켜짐 (어제 내가 넣은 것) | ConstituentSheet.tsx:53-61 (useMemo deps=[items]) | 확인 |
| D10 | 🟡 | 시황 「돈의 방향」 카드에 **기준 시각·묵음 표시가 없다** (5분 캐시 넷 조합) | components/overview/MoneyFlowPanel.tsx | 확인 |
| D11 | 🟡 | 국내 ETF 자금흐름 페이지도 기준 시각 없음 (형제 페이지는 있음) | pages/EtfFlowPage.tsx | 확인 |
| D12 | 🟡 | 현미경(매수 직전 화면) 0B 덮기에 장 시간·healthy 가드 없음(90초 나이만) | pages/ScopePage.tsx:516-556 | 그럴듯 |
| D13 | 🟡 | 거래상위도 같은 가드 빠짐 | pages/VolumeRankingPage.tsx:92-107 | 그럴듯 |
| D14 | 🟡 | 종목 상세 머리줄 — 키움이 flu_rt 를 안 주면 **NaN%** | components/StockDetail.tsx:206,228,247 | 확인 |
| D15 | 🟡 | 데일리 리포트 맨 위 새로고침 막대는 **가장 새 섹션** 시각(indices), 본문은 가장 오래된 것 | pages/DailyReportPage.tsx:399 vs :355,474 | 확인 |
| D16 | 🟡 | 머리글 지수 단추 — 섹션이 멈춰도 나이·오류 표시 없이 마지막 값 유지 | components/HeaderTicker.tsx:62,140-160 | 그럴듯 |
| D17 | ⚪ | ETF 「대금 1위」 칩이 방향과 무관하게 늘 빨강 | pages/EtfPage.tsx:141 | 확인 |
| D18 | ⚪ | 「지금」 사는 손 — boost null 이면 숫자 없는 `×` | components/MoneyNowPanel.tsx:51 | 확인 |
| D19 | ⚪ | 「지금」 60초 폴링에 순서 가드 없음 — ↻ 직후 옛 캐시가 덮을 수 있음 | MoneyNowPanel.tsx:114-135 | 확인 |
| D20 | ⚪ | ListTrack·Map·Etf 주 조회에 순서 토큰 없음(같은 부류) | ListTrackPage.tsx:147 · MapPage.tsx:138 · EtfPage load() | 확인 |
| D21 | ⚪ | 구성종목 시트 꼬리말 「시가총액 큰 순」이 등락률 정렬·재정렬 때도 그대로 | ConstituentSheet.tsx:249 | 확인 |
- 깨끗했던 것: useRealtime·useSection·useLive(정리·순서·가시성), 정렬 훅(원본 불변), 시세분석 덮기 규칙, PriceHeader 억/백만원 환산, 조건검색 조 환산, 관심종목 키 중복, **해시 링크 죽은 탭 없음**.

### E. 실측에서 나온 것 (화면 훑기·로그)
| # | 심각도 | 무엇 | 근거 |
|---|---|---|---|
| E1 | 🟠 | **네이버 선물 투자자 수급이 HTTP 410(Gone)** — `/api/market/futures-flow` 가 매 폴링 500 을 내고 서버 로그에 스택이 쌓인다(10분에 40줄). 시황 「국내 지수」 카드의 선물 수급 칸·브리핑 선물 행이 빈다. 네이버 개편으로 주소가 사라진 듯 — 대체 출처 필요(한투 선물옵션 투자자? 키움?) | 로컬 서버 로그 `naverFuturesFlow.ts:43 HTTP 410` · 09/18 08:2x |
| E1-b | 🟠 | 같은 폐쇄가 **장중 투자자 누적 수급**(`investorDealTrendTime`, 2분 간격 — 지수 상세 시트의 「장중 수급 변화」 곡선)에도. 어제 날짜로 물어도 410 → 데이터 없음이 아니라 **주소 폐쇄**. 나머지 네이버 구 금융(종목토론·주요뉴스·테마 목록/상세·ETF 목록)은 200 으로 살아 있음 | 08:25 직접 요청: Day(sosok=03)·Time(sosok=01) 둘 다 410, 다른 여섯은 200 |
| E2 | ⚪ | 화면 훑기: 52개 메뉴 × 폰 375·태블릿 768 전부, PC 1440 은 18개 — **가로 넘침·오류 경계·잘린 nowrap 0건.** (겹침·이름 충돌은 이 검사로 안 잡힘 → C 절) | window.__sweep 자동 측정 |
| E3 | ⚪ | 로컬에서 `/api/realtime/stream` 400 — 개발 PC 는 실시간이 꺼져 있어서. 미니PC 와 무관 | 네트워크 기록 |
| E4 | 🟡 | API 전수 호출(api.ts 의 매개변수 없는 GET 130개, 쓰기·무거운 18개 제외): non-200 은 **`/api/pulse/leaders/track` 60초 타임아웃** 하나. 느린 것 `/api/us-watch?force=1` 18초(강제라 예상 범위). 빈 배열 24개는 전부 「아직 데이터 없음」 부류(저널·채널·프리셋 등)라 정상 | `api_sweep.py` 08:3x · leaders/track 은 무엇을 하는지 확인 필요 |
| E5 | ⚪ | 로컬 첫 요청마다 2초 지연 — `localhost` 가 IPv6(::1)를 먼저 찔러 실패한 뒤 IPv4 로 넘어가는 Windows 버릇. 두 번째부턴 7ms. 미니PC·브라우저(vite 프록시)와 무관 | 같은 엔드포인트 3회: 2081·7·7ms |

## 체크포인트 기록
### ① 08:05 NXT 프리 (로컬)
- global 22줄 오류 0 · 선물 부호 인베스팅과 일치(−) · 체결 시각 07:5x(야후 10분 지연)
- moneyNow slot=pre, 플랜 「장 전 — 어젯밤과 주도 테마」 4항목, errors 없음
- vi-today ok, 13종목(프리마켓 VI) · etfFlow asOf=어제, d1 null 0 (장 전 폴백 동작)
- 미니PC: Tailscale 「coordination server 연결 불가」로 health 못 읽음 (#1)

## 고침 현황 (21:42 최종)
- **47c28f7 (08:36 배포)** — B1 · B2 · B16
- **c7ae4d8 (12:40 배포)** — A1 A2 A3 A4 A5 A6 A7 A8 A9 A10 A13 A15 A19 A20 A21 A22 · #2 · B4 B5 B6 B7 B8 B10 B11 B18 B21 B23 · C1 C2 C3 C4 C5 C6 C8 C17 C18 C19 C20 C22 C23 · D1 D2 D4 D5 D6 D7 D8 D9 D14 D20 D21 · E1 E1-b E4 · #4 #5 #6
- **ed9f579 (12:42 배포)** — A12 A16 A17 A23 · D3 D18 D19
- **cba9cd3 (17:43 배포) 마무리** — #10 건너뜀 사유 · A11 알림 키 되돌림 · A24 NXT 프리 08:50 · D10 D11 D12 D13 D15 D16 D17
- **결함 아님으로 판정** — A14(computeAlerts 가 이미 `tradeEok>0 && capEok>0` 로 막음 — 감사 오탐) · A18(날짜 없는 키는 「오늘」 키라 어제 것을 지우는 게 맞다)
- **보류 하나** — C24(`--ov-*` 를 :root 로 올리면 라이트·엑셀 테마에서 색이 틀어진다 — 테마별로 다시 깔아야 하는 공사)
- **2215f60 (13:20 배포)** — E1 선물 수급을 네이버 새 모바일 API(m.stock.naver.com/api/index/FUT/trend, 오늘 계약)로 갈아탐 + 일별 저장 · 장중 곡선 03(선물)도 표본
- **0263d26 (13:26 배포) 벤티지 선택 4차** — B3·B4 열린 시장 우선 · B12 손절선 지우기(0)만 비밀번호 · B15 손실만 합산 · B17 매도 감시 3회 재시도
- **11997fa (13:35 배포) 벤티지 선택 5차**
- **de53467 (15:21 배포)** — #9 마감 전 종배 후보·교차를 어제 원장 기준으로
- **4b57714 (14:20 배포)** — #8 네이버 전수조사 + 선물 수급 45일 소급
- **2188200 (13:40 배포)** — #7 선물 수급 단위 억원(네이버 새 API = 키움 앱), 환산 제거 · 신호등 문턱 ±5,000억 — B9 키움 평가손익+실시간 차분 · B13 안전 쪽(중복 제거→합→천장) · B14 호가 단위 거절+제안 · B19 cashLog+10초 중복 차단
- **결정 필요(남음)** — 없음. B13 은 부분 체결이 나오는 날 ka10076 누적 여부를 실측해 확정
- **8378d12 (19:17 배포)** — 키움 9/21 미체결 자동취소(15:40): 확인 결과 고칠 것 없음, 주석·화면 안내만
- **남은 하나** — #11 A3 과잉 교정(공매도·대차 빈 응답을 실패로 셈, 실패 137). **22:30 배포 금지 풀리면 고침**
- **TODO 로 넘김** — 네이버 새 API 는 오늘 전수조사 끝냄(결과는 docs/다음작업_TODO.md). 남은 항목은 etfItemList.nhn 폴백 하나
- **보류(잠복·큰 공사)** — B20 B25 · C7 C9 C10 C11 C12 C13 C14 C15 C16 C21 C25
- 오전 09:0x~12:30 은 세션 한도로 검증 에이전트가 멈춤 → 체크포인트 ②③ 은 기록 못 함(「못 본 것」)

### ④-0 12:52 배포 확인 (ed9f579)
- 미니PC `deploy.status` = OK 12:44:07 ed9f579 · health 12:52 uptime 483s · 국내실시간 연결됨/175 · healthy · 키움조회대기 max 223ms
- 로컬 장중 수급 표본(12:41 배포 뒤 2분 간격): 코스피 6점(12:50 외인 +7,408 · 기관 +9,114억) · 코스닥 6점(외인 −712 · 기관 +187) — 곡선 재료가 쌓인다. 미니PC 도 같은 코드라 같은 시각부터 쌓일 것(Cloudflare 뒤라 직접은 못 봄)

### ④ 13:34 장중 오후 (로컬 + 미니PC health)
- 미니PC: 0263d26 OK 13:26 · health 13:34 실시간 175 healthy · 키움조회대기 990건 max 223ms. 11997fa 는 깃발 13:34 → 다음 확인
- money-now: slot afternoon · 판정 **in**(flow +1 · program +1 · 심리 0 · 진행률 0 · 체온 0) · errors 없음 · 버킷 조용 8·돌파 1·뜨거움 8 · 사는 손 10 · 내 계좌 0줄(보유 없음, kiwoomOk·manualOk) · **추세 6점(12:38~13:29, 10분 간격 — A5 고친 뒤 정상)** · 성적표는 오늘 첫날이라 비어 있음(정상)
- 장중 수급 표본: 코스피 23점 · 코스닥 26점 · 선물 5점(13:2x 부터) — 마지막 13:32. 곡선 재료 정상
- 글로벌 fresh=1: ES +0.15 · NQ +0.13 · YM +0.14 · WTI +0.20 · VIX −12.8(어제 마감 현물) · 달러/원 +0.65 · 달러/엔 +0.67 — 세션 기준 부호(07:00 KST 시작) 일관
- 카드 ↻: flow 섹션 cached 13:34:34 → fresh=1 13:35:31(48ms, stale false) — 갱신 동작 OK
- 구성종목 시트 실시간 ●: **로컬은 실시간 꺼짐(REALTIME_ENABLED=0)이라 못 봄** — 코드(useRealtime 덮기·D9 30초 재계산)로만. 미니PC 화면은 Cloudflare 뒤라 못 봄 → 「못 본 것」

### ⑤-1 15:08 마감 전 (로컬)
- money-now: slot **closing** · 판정 **rotate**(갈아탐) · errors 없음 · 추세 12점(10분 간격, 12:38~15:0x)
- plan 「마감 전 — 종가배팅」: 종배 후보 줄이 **「오늘 스캔이 아직 없습니다」** · **cross(14:30 교차) 빈 배열** → 발견 #9 (기능이 제 시간대에 못 뜬다)
- 사는 손은 정상: 나라스페이스 ×123.2 +4.6% · 켄코아에어로 ×80.0 +22.2% · 비츠로테크 ×51.2 +0.2%
- 성적표(record): days 0 · today {10:00 null, 13:30 rotate} · 「오늘부터 쌓습니다」 안내 — **A5 고친 뒤 오늘 판정이 실제로 적히기 시작**(13:30 칸 채워짐)
- 종배 스캔 파일: `closeBetScan.json` date=**2026-09-15** green 48 universe 1404 · `failedLists/failedStocks` 없음(그날은 실패 0 — A8 새 필드는 다음 실패 때 확인)
- dayMark: `moneyNowBrief:810`(13:30)·`moneyNowBrief:905`(15:05) = 2026-09-18 → **A9 고침이 실제로 동작**(보낸 뒤 도장). 09:35·11:30 은 그 시각에 로컬이 꺼져 있어(세션 한도) 없음 — 결함 아님

### ⑤-2 15:33 마감 직후 (15:30~16:00 공백)
- **거래소 열림 판정 정상**: `/api/order/status` open = **KRX false · NXT true · SOR true** → B2(장 밖 발동 보류) 가드가 실제로 이 창을 닫는다. 감시 발동은 「열리면 다시 본다」로 보류됨
- 미니PC health 15:32: 국내실시간 연결됨/175 healthy · 저장소 day=2026-09-18 points=350,659 (하루치 정상 적재)
- 장중 수급 표본: 코스피 77점 · 코스닥 82점 · **선물 47점**(13:2x 부터) — 마지막 15:32. 16:04 에 15:40 뒤로 안 느는지 재확인 예정
- 시황 「국내 지수」 카드 배지: 정규장 → **「휴식」**(툴팁 「정규장은 끝났고 애프터마켓(16:00)은 아직입니다 — 지금은 어느 시장도 안 엽니다」) — SessionBadge 설계대로. 결함 아님
- de53467(#9 교차 수정) 15:21 배포 OK — 15:40 배포 금지 창 전에 들어감

### ⑥-1 16:04 애프터 개장
- **VI 오늘 표식 정상**: 오늘 누적 96종목 중 **애프터(16:00~) VI 39종목** — HLB테라퓨틱스 16:03:30 해제·발동가 2,595(시가대비 +26.3%), 티피씨글로벌 16:03:15 등. 9/17 에 걸렀던 「13% 상방 VI」 같은 허수는 안 보임(가격제한 ±10% 거름이 도는 중)
- 계좌 NXT 기준가 덮기: **보유 0 종목**이라 `_priceBasis` 빈 값 — 이 시간대 동작은 보유가 생긴 날 다시 본다(「못 본 것」)
- 장중 수급 표본이 **제때 멈춤**: 코스피 78점(마지막 15:34) · 코스닥 83점(15:34) · 선물 49점(15:40) — 15:40 상한이 그대로 지켜졌고 애프터엔 안 쌓인다(설계대로). 코스피·코스닥이 15:34 에서 멈춘 것은 값이 더 안 바뀌어서(같은 값이면 점을 안 늘림)
- 미니PC health 16:04: 실시간 175 healthy · **마감 뒤 정리 오늘 회차가 15:56 에 시작돼 진행 중**(실패 0, finishedAt 아직 null)
- 종배 스캔 health: 마지막 **2026-09-16** · 「종배저녁 아직 안 씀」 → 발견 #9 의 전제(오늘 스캔은 이 시간대에 없다)가 미니PC 에서도 사실로 확인됨

### ⑥-2 16:50 15:55 정리 회차 (중간)
- 오늘 회차 15:56 시작 · **finishedAt 아직 null(진행 중)** · 실패 목록 비어 있음. 지금까지 끝난 단계는 넷:
  | 단계 | ok | 걸린 시간 | 메모 |
  |---|---|---|---|
  | bars(일봉) | ✅ | 14분 10초 | 2,818종목 |
  | ledger(원장) | ✅ | 30분 37초 | **2,623/2,623 · 실패 2** · 2,629종목 |
  | regime(장세) | ✅ | 0.8초 | |
  | track | ✅ | 2초 | 0건 담음 |
  - 나머지(listTrack·super·cross·marks·condAuto·trade·finance·samples)는 아직 **9/17** — 순서대로 도는 중(listTrack 이 12분짜리)
- **A3 고침이 실제로 보인다**: 어제는 「2,624/2,624 · 실패 0」이었는데 오늘은 **실패 2** 가 잡혔다 — 200 인데 빈 봉투를 주던 종목이 이제 실패로 세어져 도장이 안 찍히고 다음 회차에 다시 받는다(예전엔 조용히 ✅). 「✅ 인데 빈 것」이 실제로 있었다는 뜻이기도 하다
- A4(일봉 전종목 실패에도 ✅) 는 오늘 2,818종목이 정상이라 발동할 일이 없었음 — 코드로만 확인. A6·A8 은 marks·종배 단계가 아직 안 돌아 ⑥-3 에서 본다
- 종배 스캔 health: 여전히 마지막 **9/16** · 「종배저녁 아직 안 씀」 — 예비 경로(18:30 뒤)라 정상. `failedLists/failedStocks` 는 그 회차가 돌아야 붙는다

### ⑥-3 17:22 15:55 회차 마무리
- **오늘 회차 12단계 전부 ✅ · 실패 목록 비어 있음 · 15:56 시작 → 17:03:49 끝(1시간 7분)**

  | 단계 | ok | 시간 | 메모 |
  |---|---|---|---|
  | bars | ✅ | 14분 10초 | 2,818종목 |
  | ledger | ✅ | 30분 37초 | 2,623/2,623 · **실패 2**(A3 효과) |
  | regime | ✅ | 0.8초 | |
  | track | ✅ | 2초 | 0건 담음 |
  | listTrack | ✅ | 12분 53초 | **1,144건** |
  | super | ✅ | 63초 | 32종목 추적 중 |
  | cross | ✅ | 4초 | 3종목 |
  | **marks** | ✅ | 2.6초 | **2,623종목** — A6(실패가 ok:true 로 둔갑) 발동 없음, 정상 종료 |
  | condAuto | ✅ | 6ms | 0식 돌림 · 건너뜀 1 → **발견 #10** |
  | trade | ✅ | 7초 | 31품목 |
  | finance | ✅ | 466초 | 2,628/2,628 받음 |
  | samples | ✅ | 16초 | 31,386관측 · 2,605종목 |

- **A20(마크 day 중앙값)**: marks 단계가 오늘 날짜로 ✅ 끝났고 그 뒤 condAuto 가 「마크가 오늘 것이 아니라」로 일찍 끊기지 **않은 정황**(아래 #10 의 애매함은 남음). 마크 파일 자체의 `day` 는 미니PC 안이라 원격으로 못 읽음
- 종배 저녁 17:00 정상 작동: 「시장 32점 < 35점 — 오늘은 안 산다」 · 후보 0 · 한 일 0 (시장문 닫힘)
- 실시간 구독 175 → **148** (저녁 정원으로 줄어듦, healthy) — 설계대로
- 15:55 회차 목표였던 「17시 안에 신호」는 **3분 초과**(17:03). finance 단계가 466초로 오늘 유난히 길었다 — 한 번 더 보고 판단

### ⑦ 20:19 마무리 회차
- **A7 실측 — 고침이 실제로 데이터를 지켰다** (`server/data/dailyCloses.json`, 000660 SK하이닉스):

  | 날짜 | c (정규장 종가) | ca (애프터 포함) |
  |---|---|---|
  | 2026-09-14 | 1,683,000 | **없음** |
  | 2026-09-15 | 1,712,000 | **없음** |
  | 2026-09-16 | 1,757,000 | **없음** |
  | **2026-09-17** | **1,745,000** | **1,766,000** ← 오늘 병합 뒤에도 살아남음 |
  | 2026-09-18 | 1,857,000 | 1,849,000 (오늘 것) |

  전 종목으로도 **9/17 봉에 ca 2,099종목 · 9/18 봉에 1,680종목**이 남아 있다. 애프터마켓이 9/14 에 시작했는데 **9/14~16 봉엔 ca 가 하나도 없다** — 그 사흘은 다음 날 병합이 어제 줄을 통째로 덮어 `ca` 를 지우고 `c` 를 애프터 포함 종가로 바꿔 놓았다는 뜻이다(A7 이 이론이 아니라 **실제로 사흘치를 망가뜨렸다**). 오늘 가드를 넣은 뒤 **9/17 부터 지켜진다**. ⚠️ 9/14~16 의 `c` 는 애프터 포함 값일 수 있어 **소급 복구가 안 된다** — 표본·백테스트에서 그 사흘을 쓸 때 유의(「못 본 것」에 남김)
- **A1 실측 통과**: 20:19 기준 `startedAt` 이 15:56(06:56:46Z) **그대로** · `finishedAt` 17:03 그대로 — 20:10 이 지나도 **정규 회차가 다시 시작되지 않았다**. 오늘 회차 12단계는 전부 9/18·ok
- 마무리 3단계(barsFinal·ledgerFinal·afterReaction)는 20:19 기준 아직 **9/17** — 19:17 배포로 미니PC 가 재시작(uptime 3,635초)한 뒤 20:10 창에 막 들어간 참. barsFinal 만 12~13분이라 20:47 에 다시 본다
- 실시간 구독 **0** (20:10 에 정상 해제) · healthy

### ⑦-2 20:47 마무리 회차 완료
- **마무리 3단계가 오늘 것으로 끝났다** — 실패 목록 비어 있음
  | 단계 | day | ok | 시간 | 메모 |
  |---|---|---|---|---|
  | barsFinal | 2026-09-18 | ✅ | 12분 29초 | 2,818종목 · **종가는 정규장 값으로 지킴** (A7 가드가 마무리 회차에서도 작동) |
  | ledgerFinal | 2026-09-18 | ✅ | 19분 59초 | 2,623/2,623 · **실패 137** → 발견 #11 |
  | afterReaction | 2026-09-18 | ✅ | 85ms | 16종목 · 애프터 값 13 · 평균 +0.36% |
- **A1 재확인 통과**: `startedAt` 여전히 15:56(06:56:46Z) · `finishedAt` 17:03 그대로 — 마무리 회차가 따로 돌았고 **정규 회차는 다시 시작되지 않았다**. 미니PC 가동 5,315초(19:18 배포 재시작 뒤)
- 실시간 구독 0 (20:10 정상 해제) · healthy

## 못 본 것
- **2026-09-14~16 일봉의 `c`** — A7 결함이 살아 있던 사흘이라 애프터 포함 종가일 수 있다. 소급 복구 불가. 그 구간을 표본·검증표에 쓸 때 유의
- 계좌 NXT 기준가 덮기(정규장 밖) — 오늘 보유가 0 이라 실측 못 함. 보유가 생긴 날 08:0x·16:0x 에 `_priceBasis=NXT` 와 키움 앱 수익률 대조
- 구성종목 시트·관심종목의 실시간 ● 표시 — 로컬 실시간 꺼짐·미니PC 화면 접근 불가. 벤티지 폰에서 한 번 확인 필요
- 체크포인트 ② 09:06 · ③ 10:41 — 세션 한도(12:30 리셋)로 못 봄. moneyNow 세 갈래·성적표·09:35 브리핑은 미니PC 텔레그램 기록으로 저녁에 확인

(채워 나감)

---

# 회귀 전수조사 — 2026-09-21 (월)

벤티지 지시: **"지난주에 총점검 한번 하고나서 더 엉망이 되엇어 전수조사 한 번 해!!"** → "빨리 다 고쳐"

범위는 **9/18 에 내가 고친 자리만**이다. 하루에 예순다섯 곳을 고쳐 배포했고, 그중 무엇이 되레
망가뜨렸는지 찾는 것이 목적이다. 원인 진단 한 줄: **고친 것을 증상으로 확인하지 않고 배포했다.**
타입 통과·눈으로 읽기만으로 「고쳤다」고 적은 자리에서 R2 처럼 **한 건도 작동하지 않은 수정**이 나왔다.

## 서버

| # | 등급 | 9/18 에 뭘 했나 | 그래서 뭐가 망가졌나 | 자리 |
|---|---|---|---|---|
| R1 | 🔴 | A1 — 마무리 회차가 정규 회차의 「끝났다」를 안 지우게 `isWrap` 예외 | 예외가 **마무리에만** 붙어, 설정에서 「①일봉만」을 손으로 누르면 그 부분 회차가 `finishedAt` 을 찍고 실패 목록을 비웠다 → 그날 15:55 정규 회차가 통째로 건너뛰어짐 | afterClose.ts:496,842 |
| R2 | 🟠 | A11 — 텔레그램 실패 시 보냄표 되돌리기 | 저장 키는 `${day}:vi:…` 인데 지울 때 `${code}:${kind}:…` 로 **새로 조립** → `delete` 가 늘 빗나가 **한 건도 못 지웠다.** 증상은 고치기 전과 동일 | liveAlerts.ts:320 |
| R3 | 🟠 | B14 — 호가 단위 안 맞으면 거절 + 맞춘 값 제안 | 코스피 **주식** 표를 전 종목에 적용 → ETF·ETN(전 구간 5원)의 **정상 주문을 우리가 먼저 거절**. 돈이 나가는 길에서 가장 나쁜 종류 | orders.ts:1250,1708 |
| R4 | 🟠 | A1 — 마무리 블록의 `return` 제거 | 마무리가 돈 날 `run` 에 마무리 결과가 남아 재시작 복구(`!run && !retry`)가 건너뛰어짐 → 두 시간짜리 **정규 회차를 처음부터 다시** (시작 알림도 다시) | afterClose.ts:991 |
| R5 | 🟡 | 장중 수급 표본을 2분마다 | `getSection` 은 만료면 **옛 값을 주고 뒤에서 갱신** → 찍히는 점이 늘 한 회차(2분) 늦음 | naverIntradayFlow.ts:71 |
| R6 | 🟡 | K200 선물도 같은 2분 박자로 표본 | 네이버 호출이 하루 200회 초과. 차단 위험은 응답 크기가 아니라 **횟수**에 붙는다 | naverIntradayFlow.ts:82 |
| R7 | 🟡 | 계좌 NXT 기준가 덮기 = 「KRX 체결 없으면」 | 밤새·주말에도 계좌 화면이 20초마다 ka10095. 그 시간엔 통합가 = KRX 종가라 **한 글자도 안 바뀐다** | routes/account.ts:58 |
| R8 | 🟡 | 돈의 흐름 「지금」 | `compute` 에 전체 상한이 없어 한 다리가 매달리면 HTTP 요청도 같이 매달림 → 아침 첫 열기가 수십 초 | moneyNow.ts:1032 |
| R9 | 🟡 | B5 — 주문 한도를 꼬리 8000줄 → 파일 전체 | 「비용은 같다」가 틀렸다. 예전엔 자른 뒤 그만큼만 파싱, 이제는 **append 전용 파일 전체**를 파싱 | orders.ts:648 |
| R10 | 🟡 | A4 — 「받을 게 있었는데 0종목」이면 실패 | `tried` 는 **이번에 시도한 것만** 센다 → 이어받기 회차에서 남은 3종목이 상폐라 빈 배열이면 `0/3` 실패. 하루치가 다 찼는데 ❌ + 헛 재시도 2회 | dailyCloses.ts:583 |
| R11 | 🟡 | A21 — 뒤집힘 알림 카운트를 「알림종에 들어간 뒤」 | 알림함 **저장이 실패하면 카운트가 안 올라 하루 2회 상한이 풀림**(텔레그램은 상한과 무관하게 5분마다 계속) · A9 로 도장을 뒤로 옮기며 넣은 `running` 참/거짓은 한 번 매달리면 **그날 남은 브리핑 전부** 차단 | moneyNow.ts:872 · moneyNowBriefing.ts:20 |

## 웹

| # | 등급 | 9/18 에 뭘 했나 | 그래서 뭐가 망가졌나 | 자리 |
|---|---|---|---|---|
| R12 | 🟠 | 슈퍼신호등 30초 얹기에 `visibilityState` 가드 | 이 앱은 **열린 인앱 탭을 언마운트하지 않는다.** 브라우저 탭이 앞에 있는 한 계속 "visible" → 한 번 열어 두면 다른 화면 보는 내내 30초마다 키움 | SuperDashboardPage.tsx:764 |
| R13 | 🟠 | 같은 얹기에서 현재가·당일만 교체 | 「편입 후」는 서버가 **같은 스냅샷 가격**으로 만든 10분 묵은 값 그대로 → 어긋남이 옆 칸으로 옮겨 갔을 뿐 | SuperDashboardPage.tsx:790 |
| R14 | 🟠 | `installApiGuard()` 로 Access 만료 입구 차단 | 호출이 `loadPrefs().then()` 안이었다. `loadPrefs` 자신이 `/api/settings/ui` 를 부른다 — **아침에 만료가 실제로 터지는 그 요청**이 가드를 안 지남 | main.tsx:27 |
| R15 | 🟡 | 같은 검사를 `api.ts` 에도 둠 | 입구 가드가 먼저 잡으므로 **절대 닿지 않는 죽은 코드** | api.ts:38 |
| R16 | 🟡 | 봉 말풍선을 좁은 화면에서 눕힘 | JS 는 `innerWidth≤720 ‖ 차트칸<520`, CSS 는 `@media(max-width:720px)` — **문턱이 두 군데.** 창은 넓고 차트 칸만 좁으면(카드 안) 격자 없이 바닥에 한 줄씩 늘어짐 | CandleChart.tsx:1015 · styles.css:6050 |
| R17 | 🟡 | 뉴스 칩에 등락률 붙이기 | 서버가 80개에서 자르는데 화면은 한 쪽 전부를 한 번에 보냄 → 81번째부터 **조용히** 등락률 안 붙음(오류도 빈 칸도 아님) | MainNewsPanel.tsx:88 |
| R18 | 🟡 | `/quotes` 주석에 「30초 캐시」 | 실제로는 `Cache-Control` 헤더뿐. 브라우저 힌트일 뿐이라 창이 여럿이거나 앱이 무시하면 **매 호출이 키움까지** | routes/market.ts:279 |

곁들여 정리: 선물 수급 시트의 죽은 삼항(`true ? … : …`, 9/18 에 억원으로 바뀌며 갈래가 사라짐) ·
`authGuard` 의 HTML 오류글에 **주소** 추가(없으면 쫓을 수가 없다).

## 실전 확인 (9/21 15:55 회차)

- 원장 **실패 2** — 9/18 밤 `ledgerFinal 실패 137` 이었다. #11(A3 과잉 교정) 수정이 맞았다는 실물 증거
- `condAuto` 메모가 **건너뛴 이유**를 찍는다 — 「수급단타 — 신호등 기준이 들어 있어 자동으로는 안 돌림」.
  9/18 까지는 「건너뜀 1」뿐이라 **정상인지 조건식 자동이 죽은 건지 구분이 안 됐다**
- 12단계 전부 ✅, 실패 목록 비어 있음 (18:42 → 19:43)

## 앞으로 (스스로 건 규칙)

1. 🔴🟠 는 **한 번에 하나씩** 고치고, **증상을 재현한 뒤에만** 「고쳤다」고 적는다 — R2 가 이 규칙이 없어서 났다
2. 🟡⚪ 는 벤티지가 말할 때만
3. 한 배포에 다섯 건 넘기지 않는다
4. 장중(09:00~15:30)에는 **로컬 서버를 띄우지 않는다** — 같은 키움 계정이라 미니PC 의 쿼터를 뺏는다
   (9/21 실측: 로컬이 하루 81,351콜 — ka90013 16,317 · ka10040 16,155 · ka10007 15,215)
