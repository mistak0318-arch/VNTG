# VNTG HTS — 다음 작업 정리

마지막 갱신: 2026-08-12

---

## 0. 세션 시작할 때 먼저 할 일

```bash
cd "K:\0000 3740 프로젝트 (경제적자유)\00. 주식\2 AI 프롬프트\260810_클로드작업" && start-all.cmd
```

- 서버 `:4000` (tsx watch — 코드 고치면 자동 재시작), 웹 `:5173`
- 세션이 끊기면 서버도 같이 죽으므로 **매번 다시 띄워야 함**
- 접속: PC `http://localhost:5173` / 폰 `http://100.88.55.69:5173` (Tailscale)

---

## 1. 우선순위 A — 미니PC 이전 준비 ★가장 급함

미니PC를 실제로 옮기기 전에 해둬야 손이 덜 감.

- [ ] **pm2 등록** — 재부팅해도 서버/웹이 자동으로 뜨게 (`pm2 startup` + `pm2 save`)
- [ ] **헬스체크 확장** — `/api/health`에 키움 토큰 유효성·마지막 조회 성공 시각 포함
- [ ] **IP 변경 감지** — Tailscale IP나 LAN IP가 바뀌면 알 수 있게
- [ ] **README 설치 절차** — 새 PC에서 `npm i` → `.env` 채우기 → `pm2` 등록까지 순서대로
- [ ] **`.env.example`** 만들기 (키 이름만, 값은 비워서)
- [ ] **키움 앱키/시크릿 재발급** — 초기에 채팅창에 한 번 노출됐음. 미니PC 옮길 때 같이 교체할 것

> 참고: 현재 `.env`에 있는 키 — `KIWOOM_APP_KEY`, `KIWOOM_SECRET_KEY`, `DART_API_KEY`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`

---

## 2. 우선순위 B — 기능

- [ ] **텔레그램 파이프라인** — 조건 만족 종목을 텔레그램으로 푸시 (미니PC 세팅 후 하기로 했던 것)
- [ ] **차트 보조지표** — RSI / MACD / 볼린저밴드. lightweight-charts에 내장이 없어서 직접 계산해 서브 시리즈로 추가. `web/src/components/CandleChart.tsx`의 `sma()` 옆에 함수 추가하는 식
- [ ] **52주 베타 / 상대수익률 차트** — 키움 일봉 데이터만으로 계산 가능 (지수 대비)
- [ ] **로그인 TOTP** — 외부 노출 시 최소 방어
- [ ] **Claude API 분석 기능** — 선택사항

---

## 3. 우선순위 C — 알려진 문제 / 미해결

### 3-1. 차트 y축 음수 눈금 (미해결)
- 증상: 캔들차트 우측 축에 `-1,000,000` 같은 음수가 표시됨
- 원인: `CandleChart.tsx:56`의 `scaleMargins: { top: 0.05, bottom: 0.3 }` — 거래량 자리를 비우려고 준 하단 30% 여백만큼 축이 0 아래로 늘어남
- lightweight-charts **v4.2.0** 사용 중. v5의 `panes` 기능을 쓰면 거래량을 아예 별도 페인으로 분리 가능 → 근본 해결
- 데이터는 정확하고 순수 표시 문제

### 3-2. 업종 PER (task #28, 계속 보류)
**결론부터: 키움 REST API로는 못 가져옴.**

- 키움 **앱**의 업종PER은 FnGuide에서 사와서 앱에 심은 벤더 데이터. 그래서 앱엔 있고 API엔 없음
- `ka10026`은 고/저PER 종목만 주고 업종 평균은 안 줌
- DART에도 없음 (개별 기업 재무제표만, 업종 집계 없음)
- **FnGuide 크롤링 불가** — `comp.fnguide.com/robots.txt` = `Disallow: /`
- **네이버 금융도 불가** — `finance.naver.com`, `m.stock.naver.com` 모두 `Disallow: /`

남은 선택지:
1. **KRX 정보데이터시스템** — 업종별 PER/PBR/배당수익률을 매일 공표. robots.txt가 404(제한 선언 없음)라 가능성 있음.
   엔드포인트 후보: `POST http://data.krx.co.kr/comm/bldAttendant/getJsonData.cmd`
   `bld=dbms/MDC/STAT/standard/MDCSTAT03501`, `mktId=STK`, `trdDd=YYYYMMDD`
   **⚠ 미확인** — 이 PC에서 curl 시도 시 응답이 없었음. 네트워크 문제인지 차단인지 확인 안 됨. **다음에 이것부터 확인할 것**
2. 업종 구성종목(ka20002) 순회 + DART 순이익으로 직접 계산 — 업종당 수십 회 호출, 무거움.
   `server/src/sectorPer.ts`에 코드는 이미 있으나 **어느 라우트에도 연결 안 됨(dead code)**. 안 쓸 거면 삭제할 것

### 3-3. 키움 앱에는 있는데 우리가 못 만드는 것
- **실시간수급(파워맵)** — 키움 자체 추정치 상품. REST API에 없음
- **종목투자자잠정 시간대별 누적** — `ka10066`은 전 종목 스냅샷 1건만 줌. 시간대별 누적을 만들려면 우리가 직접 폴링해서 쌓아야 함
- **거래원 외국계합** — `ka10002`가 상위 5개 창구만 줌. 현재 "상위5 내 합계"로 표시하고 화면에 명시해둠

---

## 4. 이번 세션에서 완료한 것 (참고)

- 재무 시각화 (매출·영업이익·순이익 3년 + 배당) — DART `fnlttSinglAcnt`
- **개별종목분석 페이지 신설** — 차트/호가/거래원/투자자/프로그램/신용/체결강도/일별상세 8탭
- 종목 상세 → 개별종목분석 이동 버튼
- 시세 요약 바 (시가·고가·저가에 등락률 % 병기)
- 업종·테마 분위기 표시
- 뉴스 주요 언론사 필터 (60개 언론사 사전 + 광고성/중복 제거)
- 타입 에러 4건 수정 → `npm run build` 통과 상태

### 이번에 알아낸 키움 API 요령 (중요)
- **`ka90001` + `qry_tp=2` + `stk_cd`** → 그 종목이 편입된 **테마만** 반환. 테마 전체를 훑을 필요 없이 호출 1번
- **`ka10099`(전종목 리스트)에 `upName`(업종명), `upSizeName`(대/중/소형주)** 포함 → 업종 매칭에 추가 호출 불필요
- TR별 **초당 5회** 제한. 초과 시 HTTP 429 + `return_code: 5` → `kiwoomClient.ts`에 백오프 재시도 구현돼 있음
- 리소스 경로가 TR마다 다름. 틀리면 `1504: 해당 URI에서는 지원하는 API ID가 아닙니다`
  - `/api/dostk/stkinfo` — ka10001, ka10002, ka10013, ka10015, ka10059, ka10099
  - `/api/dostk/mrkcond` — ka10004, ka10007, ka10046, ka10047, ka10063, ka10066, ka90013
  - `/api/dostk/chart` — ka10060, ka10080~83
  - `/api/dostk/sect` — ka20002, ka20003 / `/api/dostk/thme` — ka90001, ka90002
  - `/api/dostk/frgnistt` — ka10008, ka10009 / `/api/dostk/shsa` — ka10014 / `/api/dostk/slb` — ka20068

### 디버깅 팁
- PowerShell은 HTTP 에러 본문을 못 읽음 → **`curl.exe` 쓸 것**
- 한글 쿼리는 셸에서 깨짐 → `node -e "encodeURIComponent(...)"`로 인코딩해서 넘길 것
- 새 TR 탐색할 땐 `server/src/_tmpTest.ts` 만들어서 `npx tsx src/_tmpTest.ts` (쓰고 나면 삭제)


---

# 📰 뉴스레터 기능 (2026-08-13 요청) — 텔레그램과 함께 최우선

인포스탁 주식신문 같은 형식의 리포트를 **하루 3회** 사용자 네이버 메일로 발송.

## ⚠ 먼저 확인할 것 — 인포스탁 크롤링은 하지 않는다

인포스탁은 **유료 구독 서비스**이고, 페이지에 이렇게 명시돼 있다:

> "본 주식신문은 인포스탁 유료회원에게 제공되는 정보이며, 본 컨텐츠를 캡쳐하여
> 온라인상에 게시하거나 공유하는 행위는 저작권법 위반으로 고발조치 됩니다."

따라서 **인포스탁 내용을 긁어와 재배포하는 방식은 구현하지 않는다.**
개인 메일로 보내는 것도 재배포에 해당한다.

**대신 같은 목적을 합법적으로 달성한다** — 형식·구성은 참고하되,
데이터는 우리가 이미 쓰고 있는 정식 소스에서 직접 모아 **자체 리포트**를 만든다.
형식(목차 구성, 섹션 배열)은 저작권 대상이 아니므로 비슷하게 꾸며도 된다.

## 발송 시각 (인포스탁 조간/주간/석간 대응)

| 시각 | 이름 | 내용 |
|---|---|---|
| 07:00 | 조간 | 밤사이 미국·유럽 마감, 오늘 일정, 관심종목 체크포인트 |
| 11:30 | 주간(장중) | 오전장 지수·테마·특징주·외국계 수급 |
| 18:00 | 석간 | 국내 마감, 테마별 등락, 특징주, 투자자·프로그램 수급, 공시 |

## 데이터 소스 (전부 기존 자산 재사용)

| 섹션 | 출처 | 상태 |
|---|---|---|
| 국내외 주요 지수 | Yahoo(globalMarket.ts) + 키움 ka20001 | ✅ 구현됨 |
| 국내 증시 마감 동향 | 키움 지수·투자자 수급 | ✅ 구현됨 |
| 특징 테마 및 테마별 등락률 | 키움 ka90001/ka90002 | ✅ 구현됨 |
| 특징 종목·상한가·급등주 | 키움 ka10027 등 | ✅ 구현됨 |
| 외국인/기관 순매수 상위 | 키움 ka10062 등 | ✅ 구현됨 |
| 공매도·신용 상위 | 키움 ka10014/ka10013 | ✅ 구현됨 |
| 프로그램 매매 동향 | 키움 ka90005/ka90013 | ✅ 구현됨 |
| 주요 종목 공시 | DART | ✅ 구현됨 |
| 뉴스 클리핑 | 네이버 검색 API (주요 언론사 필터 적용) | ✅ 구현됨 |
| 52주 신고가 | 키움 ka10016 | ✅ 구현됨 |

**즉 데이터는 이미 다 있다.** 새로 만들 것은 "모아서 → 요약해서 → 메일로" 부분뿐.

## Claude API 활용 (결제 완료됨)

- 뉴스 제목 수십 건 → **핵심 이슈 3~5줄로 요약**
- 테마 등락 데이터 → **"오늘 왜 올랐나" 한 문단**
- 토큰 절약: 원문 전체를 넣지 말고 **제목 + 등락률 숫자만** 전달
- 모델: `claude-sonnet-5` (요약 용도엔 충분). 비용 민감하면 `claude-haiku-4-5-20251001`
- 실패해도 메일은 나가야 함 → 요약 실패 시 원본 데이터만 넣어 발송

## 메일 발송 — 네이버 SMTP

```
호스트: smtp.naver.com
포트: 465 (SSL)
계정: 네이버 아이디 / 비밀번호
```
- ⚠ 네이버 메일 설정에서 **POP3/SMTP 사용 '사용함'** 으로 먼저 켜야 함
- `nodemailer` 사용 (의존성 1개 추가)
- `.env`에 `NAVER_MAIL_USER`, `NAVER_MAIL_PASS`, `MAIL_TO` 추가
- **비밀번호는 사용자가 직접 .env에 입력한다** (Claude가 대신 입력하지 않음)

## 구현 순서

1. `server/src/mailer.ts` — nodemailer + 네이버 SMTP, `POST /api/mail/test`로 **테스트 메일부터 성공**
2. `server/src/report.ts` — 섹션별 데이터 수집 → HTML 조립 (인포스탁처럼 표 중심)
3. Claude API 요약 붙이기 (`server/src/summarize.ts`)
4. 스케줄러에 07:00 / 11:30 / 18:00 등록 (텔레그램 스케줄러와 공용)
5. 설정 화면에서 on/off, 수신 주소, 테스트 발송 버튼

> 텔레그램 봇의 스케줄러·설정 저장 구조를 그대로 재사용한다.
> **텔레그램을 먼저 만들고 뉴스레터를 얹는 순서가 효율적이다.**

## 주의

- 메일 본문에 **뉴스 원문 전문을 넣지 않는다.** 제목 + 언론사 + 링크만 (현재 뉴스 탭과 동일 원칙)
- 나에게만 보내는 개인용. 타인에게 전달·공유 기능은 만들지 않는다
- 발송 실패가 서버를 죽이면 안 됨 → try/catch + 로그


---

# 🌏 미국↔한국 테마 연동 (2026-08-13 요청)

밤사이 미국 시장의 특정 섹터가 움직이면 다음날 국내 연동 테마가 따라가는 패턴을
자동으로 짚어준다. **조간 뉴스레터의 핵심 섹션이 될 기능.**

## 왜 필요한가
사용자 표현 그대로 — "글로벌 흐름에 의한 국내시장도 추격하니깐".
밤사이 NVDA가 급등했으면 아침에 반도체 관련주를 먼저 봐야 하는데,
지금은 미국 지수만 보여줄 뿐 **"그래서 오늘 국내 뭘 보나"가 없다.**

## 구현 방향

### 1. 매핑 테이블 (수동 큐레이션이 현실적)
`server/src/usKrLink.ts` 에 US 티커/섹터 → 키움 테마코드 매핑을 JSON으로.
자동 상관계수 계산도 가능하지만, 초기엔 손으로 넣는 게 정확하고 빠르다.

예시 (실제 테마코드는 ka90001로 조회해서 채울 것):
| 미국 | 국내 연동 테마 |
|---|---|
| NVDA, AMD, TSM, MU | 반도체 대표주, HBM, 반도체 장비/소재 |
| 필라델피아 반도체지수(SOX) | 반도체 전반 |
| TSLA, 리튬/배터리 | 2차전지, 전기차 부품 |
| 코어위브, 슈퍼마이크로, 데이터센터 | 전선/전력설비, AI 인프라 |
| 일라이릴리, 노보노디스크 | 비만치료제, 바이오시밀러 |
| 록히드마틴 등 방산 | 방산, 우주항공 |
| 팔란티어, MSFT AI | AI 소프트웨어, 챗봇 |
| WTI/에너지 | 정유, LPG, 조선(LNG선) |
| 금/은 | 금 관련주 |

### 2. 데이터
- **미국 등락**: Yahoo (`globalMarket.ts` 확장). 개별 티커 추가는 심볼만 넣으면 됨.
  ※ **필라델피아 반도체지수(^SOX)** 를 꼭 추가할 것 — 국내 반도체와 상관성이 가장 높다
- **국내 테마 등락**: 키움 ka90001 (이미 구현됨)
- 둘 다 **기존 자산 재사용**. 새 API 계약 불필요

### 3. 화면 / 알림
- **시황 탭에 "글로벌 → 국내 연동" 카드**: 밤사이 오른 미국 섹터 옆에 대응 국내 테마와
  그 테마의 오늘 등락률을 나란히. "미국 반도체 +2.1% → 국내 반도체 테마 +5.4% (따라감)"
- **조간 뉴스레터 상단 섹션**: 오늘 주목할 테마 3개를 근거와 함께
- 텔레그램 조간 알림에도 한 줄 요약

### 4. 한 단계 더 (선택)
- 실제 **상관계수를 계산해서 매핑을 검증**: 미국 티커 일봉 vs 국내 테마 일별 등락률의
  60일 상관계수. 0.5 미만이면 "연동 약함"으로 표시하거나 매핑에서 제외
- 이렇게 하면 하드코딩 매핑이 낡는 문제를 자동으로 잡아낼 수 있다

## 주의
- **상관관계는 시간이 지나면 변한다.** 매핑을 고정 진리로 표시하지 말 것.
  "과거 이런 경향이 있었다" 수준으로 표현하고, 가능하면 상관계수를 같이 보여준다
- 미국 시장이 쉬는 날(현지 휴장) 처리 필요
- 이건 **참고 지표이지 매매 신호가 아니다.** 화면 문구에 그 톤을 유지할 것


---

# 📊 API 사용량 추적 확장 (텔레그램/뉴스레터와 함께 반드시)

`server/src/apiUsage.ts`의 provider 목록에 아래를 **반드시 추가**한다.
빠뜨리면 실제로 돈이 나가는 Claude API를 못 보게 된다.

| provider | 한도 | 비고 |
|---|---|---|
| `anthropic` | 없음(종량제) | **호출 수 + 입력/출력 토큰까지 기록.** 다른 API와 달리 비용이 직접 발생 |
| `telegram` | 초당 ~30건 | sendMessage 성공/실패 |
| `mail` | 네이버 일일 발송 제한 있음 | SMTP 발송 성공/실패 |

## anthropic은 특별 취급
- 다른 provider는 "호출 횟수"만 세면 되지만, Claude는 **토큰이 비용**이다
- 응답의 `usage.input_tokens` / `usage.output_tokens`를 같이 누적할 것
- 설정 화면에 **누적 토큰 + 추정 비용(모델별 단가 × 토큰)** 표시
- 하루 예산 상한을 두고, 넘으면 요약을 건너뛰고 원본 데이터로만 리포트 발송하는
  안전장치를 고려 (무한 과금 방지)

## 구현 위치
- `apiUsage.ts` — provider 정의 + 토큰 누적 필드 추가
- `summarize.ts` — Claude 호출 후 `recordApiCall("anthropic", model, "ok", {inputTokens, outputTokens})`
- `SettingsPage.tsx` — 카드 3개 추가, anthropic 카드엔 토큰/비용 표시


---

# 🆕 추가 기능 요청 (2026-08-13)

사용자가 정리해 준 5개 항목. **실현 가능성 판단을 각 항목에 붙여둠.**

## 1. HTTPS 전환 — ✅ 가능 (Tailscale 경유에 한해)

**현재 상태 정정**: Tailscale 주소(`100.x.x.x`)로 접속할 때는 겉보기만 HTTP이고
실제 트래픽은 **WireGuard로 암호화**된다. 이미 도청 위험은 없다.

그래도 진짜 HTTPS를 원하면:
- `tailscale cert` + MagicDNS → **Let's Encrypt 정식 인증서**를 `*.ts.net` 도메인으로 발급 가능.
  브라우저 경고 없이 자물쇠가 뜬다. 이게 정답
- 절차: Tailscale 관리 콘솔에서 MagicDNS + HTTPS 켜기 → `tailscale cert <머신>.<tailnet>.ts.net`
  → Express를 `https.createServer`로 띄우거나 `tailscale serve` 사용
- **LAN 주소(192.168.x.x)는 자체서명뿐** → 브라우저 경고 발생. 굳이 할 필요 없음
- ⚠ **공유기 포트포워딩은 절대 하지 말 것.** 이건 HTTPS 여부와 무관하게 위험

## 2. 캘린더 (마이페이지) — ✅ 가능 / 구글 연동은 △

- **직접 입력 캘린더**: `server/data/calendar.json` + CRUD 라우트 + 월/주 뷰. 어렵지 않음
- **증시 일정 프리셋**: FOMC/CPI/옵션만기/실적시즌 등을 JSON으로 넣어두고 매년 갱신
  (무료 경제캘린더 API가 마땅치 않음. Investing.com은 크롤링 금지)
- **구글 캘린더 연동**:
  - 양방향 동기화(OAuth)는 복잡 — 구글 클라우드 프로젝트 생성, OAuth 동의화면,
    토큰 갱신 처리까지 필요. **권장하지 않음**
  - **대안: iCal(ics) 구독 URL 읽기** — 구글 캘린더의 "비공개 주소(ICAL)"를 `.env`에 넣고
    서버가 주기적으로 파싱해서 읽기 전용으로 표시. **훨씬 간단하고 목적 달성엔 충분**
  - 먼저 iCal 방식으로 하고, 정말 쓰기가 필요하면 그때 OAuth 검토

## 3. 관심종목 그룹 — ✅ 가능 (쉬움)

- 현재 `watchlist.json`은 평면 배열 → `group` 필드 추가
- 종목 추가 시 그룹 선택 드롭다운 (없으면 "기본")
- 그룹 생성/이름변경/삭제, 그룹별 탭 보기
- 기존 데이터는 마이그레이션 시 전부 "기본" 그룹으로

## 4. 테마 / 폰트 설정 — ✅ 가능 (라이트 테마는 손이 좀 감)

- **폰트 크기·글꼴**: `:root`의 `font-size`와 `font-family`만 바꾸면 되므로 쉬움.
  localStorage에 저장
- **라이트 테마**: `styles.css`가 이미 `--bg`, `--panel`, `--text` 등 CSS 변수를 쓰고 있어
  구조는 준비돼 있음. 다만 **하드코딩된 색상이 남아 있는지 전수 점검 필요**
  (특히 차트 컴포넌트 — `CandleChart.tsx`, `TrendLineChart.tsx`에 `#1a232d` 등이 박혀 있음)
- 차트 색상도 테마에 따라 바뀌도록 변수화해야 완성도가 나옴

## 5. 계좌 — 연동 / 수동 분리 — ✅ 가능 (가치 높음)

### 구조
- 계좌 탭을 **"연동 계좌"** / **"수동 계좌"** 로 분리. **통합 자산현황은 만들지 않는다**(사용자 요청)

### 연동 계좌
- 지금 그대로. 키움 API로 잔고·평가손익 자동 조회

### 수동 계좌
- `server/data/manualAccounts.json`
  ```
  { id, broker: "미래에셋", name: "연금저축", holdings: [ { code, name, avgPrice, qty } ] }
  ```
- 증권사 목록은 하드코딩 (미래에셋/삼성/NH/한국투자/KB/신한/토스 등)
- 종목 검색은 기존 `api.searchStocks` 재사용
- **평가금액·손익·수익률은 저장하지 않고 조회 시 계산** — 현재가만 키움에서 가져와
  `(현재가 - 평단) × 수량`. 저장하면 값이 낡는다
- 증권사별 탭 또는 카드 리스트

### 주의
- 이 앱은 **조회 전용**이다. 수동 계좌도 사용자가 직접 적는 메모일 뿐,
  주문·이체 기능은 만들지 않는다
- 시세 조회가 종목 수만큼 발생 → **한 번에 모아서 조회하고 캐싱**할 것 (TR당 초당 5회 제한)

---

## 우선순위 제안

기존 항목과 합쳐서:

1. **텔레그램 봇** (준비 완료, 바로 가능)
2. **뉴스레터** (텔레그램 인프라 재사용)
3. **수동 계좌** — 실사용 가치가 가장 큼. 타 증권사 앱 안 열어도 됨
4. **관심종목 그룹** — 쉽고 체감 큼
5. **캘린더** (iCal 방식) → 미국↔한국 테마 연동
6. **테마/폰트** — 급하지 않음
7. **HTTPS** — Tailscale 쓰는 한 이미 암호화됨. 마지막에


---

# ✅ 2026-08-13 완료 목록 (이 날 작업분)

- **관심종목 그룹** — 그룹 CRUD, 그룹 탭 필터, 행별 드롭다운 이동.
  기본 그룹 보호(삭제·이름변경 불가), 그룹 삭제 시 소속 종목은 기본으로 이동
- **수동 계좌** — `계좌 > 수동 계좌`. 증권사 13곳, 계좌 여러 개, 종목 검색 후 평단·수량 입력.
  평가금액·수익률은 저장하지 않고 조회 시 계산. 기존 계좌는 `연동 계좌 (키움)`로 개명.
  통합 자산현황은 만들지 않음(요청대로)
- **데일리 리포트** — `시황 > 데일리 리포트`. 7개 섹션(지수/수급/테마/업종/특징주/신고저가/뉴스).
  조간·장중·석간 토글은 라벨만 있고 섹션 차등화는 미구현.
  **기존 시황 캐시를 공유해서 추가 API 호출 0**
- **테마/글꼴/글자크기** — `설정 > 화면 설정`. 다크/라이트, 글꼴 5종, 크기 5단계.
  localStorage 저장(기기별). 차트는 CSS 변수를 못 읽어 `chartColors(theme)`로 별도 처리
- **Claude API 토큰·비용 추적** — `summarize.ts` + 사용량 화면에 토큰/추정비용 표시
- **시황 카드 순서** — 종목등락현황을 글로벌 위로
- **글로벌 시황** — 인베스팅 배열 순서로 재정렬, 이더리움/은/구리/천연가스/코스닥 추가

## 다음 세션 최우선

1. **캘린더 (직접 입력)** — 유일하게 남은 미착수 항목.
   `server/data/calendar.json` + CRUD + 월/주 뷰. 구글 연동은 iCal 읽기 전용으로 나중에
2. 텔레그램 봇 → 뉴스레터 발송 (설계서 있음, 키 검증 완료)
3. 데일리 리포트 판(조간/장중/석간)별 섹션 차등화

## 주의: 라이트 테마 추가 시 확인할 것
`overview.css`는 `.ov` 스코프에 자체 변수(`--ov-*`)를 선언한다.
`:root`에만 오버라이드하면 안 먹으므로 `:root[data-theme="light"] .ov` 로 타깃해야 한다.
새 CSS 파일을 만들 때 색상을 하드코딩하면 라이트 테마가 깨진다 — 반드시 변수 사용.
